# ============================================================
# InfoData 环境变量模板（docker compose 使用）
# 复制为 .env 后填写，.env 已被 .gitignore 排除
#
# 模式 A（默认，直连本机 MySQL）：保持 host.docker.internal
# 模式 B（全容器化）：INFO_DATA_DB_HOST=mysql / USER=infodata / PASSWORD=infodata123
# ============================================================

# ---- 数据库连接（backend 使用） ----
INFO_DATA_DB_HOST=host.docker.internal
INFO_DATA_DB_PORT=3306
INFO_DATA_DB_USER=root
INFO_DATA_DB_PASSWORD=root
INFO_DATA_DB_NAME=adata

# ---- 容器版 MySQL（仅模式 B 使用） ----
MYSQL_ROOT_PASSWORD=root123456

# ---- 可选数据源 ----
TUSHARE_TOKEN=

# ---- AI 分析（方舟豆包） ----
ARK_API_KEY=
ARK_MODEL_ID=doubao-1-5-pro-32k-250115
