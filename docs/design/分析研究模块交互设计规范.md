# InvestBuddy 分析研究模块 交互设计规范 v0.9（定稿）

> 版本：2026-09-19 · 状态：**v0.9 定稿**（落地 §1.1 第 4 条「口径透明」在**总览页卡片墙**的缺失入口 ——
> 后端 28 个 KPI 的 `hint` 早已随响应下发（覆盖率 100%，最长 243 字）、详情页也早已消费，
> 但卡片墙的 `CardKpi` 从未声明该字段，导致「数字看得到、口径看不到」。
> 本轮抽出共用件 `KpiHint.vue`（ⓘ 描边入口 + 白底信息卡 + RichText 渲染 `**强调**`）两页统一，
> 顺带修掉详情页 tooltip 的 `**` 直出缺陷。见 §14）
> 版本：2026-09-17 · 状态：**v0.8 定稿**（**排期契约二次修正：固定时刻 → 链式触发**。
> `market_style_sync` / `market_current_sync` / `data_quality_check` 的固定时刻降为**兜底**（21:30/21:40/21:50，
> 当日已成功则跳过），主触发改为「日线完成 → 自动接力」（`scheduler.CHAIN_NEXT`）；
> 全部任务时刻收进本机在线窗口 19:00–23:00，日线 19:00 → **19:20** 让前置链跑完。
> 另：快照护栏拒绝由 `failed` 改为 **`blocked`**（三态语义，不计失败率），
> 物化表上界回退写 `stale_upper_bound` 显式标注。见 §13、§4.1）
> 版本：2026-09-16 · 状态：**v0.7 定稿**（修正**盘后排期契约**：`market_style_sync` 18:45→**20:05**、
> `market_current_sync` 19:30→**20:15**，两者输入均为个股日线而日线常态耗时 15~50 分钟，
> 原时刻必然读到半量日线；并加**双重充分性护栏**（不足上一交易日 90% 即拒绝/退回），
> 见 §13。§4.1 的排期契约同步订正）
> 版本：2026-09-15 · 状态：**v0.6 定稿**（落地 §1.0 第一原则的 **P1「零采集成本」阶段**：补上第四类参照系
> **④交叉印证** 整类空白（`cross_check.py` 六维）＋ 新增模块 **板块轮动**（①同类横比 + ③基准超额 + 双侧口径互证），
> 并把「换手率」物化为 `turnover_med`、修正源列单位漂移，见 §12）
> 版本：2026-09-15 · 状态：**v0.5 定稿**（新增 **§1.0 第一原则「数据用来比较才有意义」**——五类参照系 + 三类背离 + 三条硬性约束；
> 并随附数据蓝图探索报告 `docs/市场风向分析数据蓝图探索报告_2026-09-15.md`，见 §11）
> 版本：2026-09-15 · 状态：**v0.4 定稿**（在 v0.3 之上为「市场风向」补**风险调整**列：收益差 ÷ 其自身滚动波动率，修正两条腿波动率不对称导致的尺度漂移，见 §4.1 / §10）
> 版本：2026-09-14 · 状态：**v0.3 定稿**（在 v0.2 框架之上，市场风向模块补齐「市场宽度 + 量能 + 标准化 + 结构性增强」四批增强，见 §9）
> 版本：2026-09-13 · 状态：**v0.2 定稿**（三个决策点已确认：总览=卡片墙 / 旧分析=下线保留码 / 注册=配置文件先行）
> 前置：旧「分析研究」页（股息率排行/概念排名/YTD 三个 Tab）定位粗糙，**先下线不考虑**；
> 本规范面向"分析研究内容会持续增加"的现实，**框架先行**——先定扩展机制，再逐个填充模块。

---

## 0. 一句话定位

> **分析研究 = 一个可持续插入分析模块的工作台**。
> 框架解决三件事：模块怎么组织（导航）、模块长什么样（统一布局）、模块怎么接入（注册机制）。

---

## 1. 数据比较框架与设计原则

### 1.0 第一原则：数据用来比较才有意义（2026-09-15 确立）

> **单个数字不是信息，只是噪声；只有放进一个比较关系里，它才成为信号。**
>
> 「今日成交 1.57 万亿」说明不了任何问题——是高是低？比昨天多还是少？在近一年处于什么位置？跟谁比？
> **没有参照物的数字不上线。** 这条原则先于本规范其余所有条款，用于**设计**（先问「跟谁比」）也用于**验收**（数字旁边有没有参照物）。

**五类参照系** —— 任何指标至少要落进一类，落不进的不予接入视图：

| # | 比较类型 | 参照物 | 回答的问题 | 本项目已落地实例 | 待补实例 |
|---|---|---|---|---|---|
| ① | **同类横比** | 同层级的不同对象 | 谁强谁弱？ | 六组指数 20 日收益、大小盘五档梯度、**申万行业 31/131 排行、概念 375 排行**（v0.6） | — |
| ② | **自身纵比** | 自身的历时序列 | 现在算不算极端？ | `pct` 近 250 日分位、`z`、`adj` 风险调整、**各印证维度的 20 日变化分位** | 估值分位、两融余额分位 |
| ③ | **基准超额** | 恰当的基准（非绝对收益） | 是真强，还是水涨船高？ | `sentiment_20`（券商−全指）、`policy_excess_20`（地产−全指）、**行业相对 `ret_bench_20` 超额**（v0.6） | 概念口径超额 |
| ④ | **交叉印证** | 另一个独立维度 | 有没有被别的口径证实？ | **六维全套（v0.6）**：杠杆 风偏×两融 / 股债 大势×10Y / 股商 周期组×商品基差 / 口径 指数×概念 / 微观 涨跌停×换手率 / 量价 大盘×量比 | 海外（恒生/纳指）、汇率、Shibor |
| ⑤ | **结构分解** | 内部构成 | 是普涨，还是少数拉动？ | 涨跌家数、60 日新高新低、均线参与度、**行业/概念上涨占比 + 离散度 + 首尾差**（v0.6） | 换手率结构、涨停/连板结构 |

**比较的最高级用法是「背离」** —— 两个本该同向的维度不同向，才是可交易的信息：

| 背离 | 读法 | 落地状态 |
|---|---|---|
| **指数 × 个股** | 指数涨而宽度差 = 少数权重股拉动的虚涨 | ✅ 已具备（`ret_bench_20` × `breadth_up_ratio`） |
| **量价** | 涨而缩量 = 虚涨；跌而缩量 = 抛压衰竭 | ✅ **v0.6 已具备**（大盘 20 日收益 × `amount_ratio_20`，成对判据 + 死区方向法） |
| **股债** | 利率创新低而股市创新低 = 流动性未传导 | ✅ **v0.6 已具备**（`cn_bond_10y` × `bench_pos_pct`；无估值分母故**不做 ERP**） |
| **股商** | 商品基差走强而周期股走弱 = 实体改善未被股价反映 | ✅ **v0.6 已具备**（`futures_spot_price` 基差 z × 对应风格组收益） |
| **杠杆** | 风偏骤降而两融仍处高位 = 杠杆盘固执 | ✅ **v0.6 已具备**（`risk_appetite_20` × 两融余额） |
| **口径** | 指数口径与概念口径打架 = 没有一致的市场叙事 | ✅ **v0.6 已具备**（六组中位 × 概念中位；两模块各自复用） |

**三条硬性约束**（写进评审清单）：

1. **无参照物不上线**：新增任何 KPI/指标，必须同时声明它的参照物（分位 / 超额 / z / 同类 / 基准），否则不予接入视图层。
2. **口径随响应下发**：口径说明由后端下发（如 `ADJ_NOTE` / `adj_note`），前端**只透传不手抄**——避免前后端各写一份而漂移（§4.2 已实践）。
3. **优先落地「能产生背离信号」的指标对**：单个指标只能描述状态，成对指标才能给出判断；评审时优先给「成对且可比」的方案排队。

### 1.1 设计原则（5 条）

1. **注册即扩展**：新增一个分析 = 后端加一个模块 + 注册一条元数据；前端框架零改动（内置视图组件拼装）。
2. **跟踪与研究分流**：跟踪型（每日自动更新、看状态变化）与研究型（按需查参数、下钻明细）交互不同，不硬塞一个模板。
3. **结论先行**：每个模块首屏必须先给"结论数字/信号"，图表和明细都是论据——分析页不是数据页。
4. **口径透明**：每个指标旁边可查计算口径（tooltip/口径卡），与设计规范的指标定义一一对应，杜绝"数字怎么来的"黑箱。
   （2026-09-19：**总览页卡片墙 + 详情页两处统一落地**，共用 `KpiHint.vue`；此条此前只在详情页兑现，卡片墙一直缺入口，见 §14）
5. **下钻闭环**：分析模块之间、分析→行情/概念/个股之间可跳转，延续"指数→行业→概念→个股"的下钻链。

---

## 2. 信息架构（两层）

```
分析研究（/analysis）
├── 总览页 /analysis            —— 跟踪型模块的卡片墙 + 研究型模块的入口目录
└── 详情页 /analysis/:moduleId  —— 单个分析模块的完整视图（AnalysisShell 容器）
```

### 2.1 总览页（默认落地页）

- **跟踪卡片区**：每个跟踪型模块一张摘要卡（名称 + 关键信号数字 + 一句状态 + 更新时间），
  例如「市场风向」卡显示：风偏分数 +6.5 / 剪刀差 +3.2pp / 状态标签「风偏升温」。
  每天 10 秒扫一眼，点卡片进详情。
- **目录区**：研究型模块按分组列出（名称 + 一句话说明），点击进详情。
- 分组固定五类（后续新增模块必须归入其一）：
  ① 市场风向 ② 板块与概念 ③ 个股基本面 ④ 资金与情绪 ⑤ 跟踪清单

### 2.2 详情页（AnalysisShell 统一容器）

所有模块共用同一骨架，**从上到下固定五段**：

| 段 | 内容 | 说明 |
|---|---|---|
| ① 标题区 | 模块名 + 口径说明（? tooltip）+ 数据截至时间 + 所处分组面包屑 | 截至时间来自物化表 MAX(trade_date)，滞后会变琥珀色 |
| ② 参数区 | 周期/日期等参数 chips（NSelect/DatePicker），改变即刷新 | 参数 schema 由模块元数据声明 |
| ③ 结论区 | 2~4 个 KPI 结论卡（大数字 + 状态词），**金色仅用于系统产出的亮点信号** | 红涨绿跌仅出现在行情数字 |
| ④ 主视图 | 模块主图表（从内置视图组件中选 1~2 个拼装） | 见 §4 视图组件清单 |
| ⑤ 明细区 | 可展开的数据表 + 下钻按钮（跳行情/概念/个股） | 表格列由模块声明 |

---

## 3. 扩展机制（框架核心）

### 3.1 后端：模块注册表（registry）

- 新表 `analysis_registry`（或先配置文件）声明每个模块的元数据：

```json
{
  "module_id": "market-wind",
  "name": "市场风向",
  "group": "市场风向",
  "kind": "track",              // track=跟踪型 / research=研究型
  "desc": "六组指数风向 + 大小盘剪刀差 + 风偏分数",
  "api": "/api/analysis/market-wind",
  "params_schema": [ {"key":"window","type":"select","options":[5,20,60],"default":20} ],
  "views": ["group-heat-bar", "size-gradient", "wind-gauge", "dual-line"],
  "drilldown": [ {"label":"看K线","target":"market"} ]
}
```

- API 约定：`GET /api/analysis/registry`（总览页目录用）+ `GET /api/analysis/<module_id>`（模块数据，统一返回 `{as_of, kpis[], main_view, detail_table}` 骨架）。
- **新增模块的标准动作**：`app/analysis/` 加计算模块 → registry 注册 → 完毕。后端到前端一条链，无散落改动。

### 3.2 前端：组件注册表 + 内置视图库

- 路由 `/analysis/:moduleId` 动态匹配；`viewsById` 映射表把 module_id 指到对应视图组件（缺失则显示"模块开发中"占位）。
- **内置视图组件**（先建 6 个，覆盖绝大多数分析形态，新模块优先复用）：

| 组件 | 形态 | 适用 |
|---|---|---|
| `KpiCards` | 2~4 张结论大数字卡 | 所有模块的结论区 |
| `GroupHeatBars` | 分组水平热力条（红涨绿跌） | 六组收益、行业对比 |
| `SizeGradient` | 有序梯度条（超大盘→微盘） | 市值风格、规模对比 |
| `DualLineTrend` | 双轴/双线时序 + 拐点标注 | 剪刀差、风偏分数等轮动序列 |
| `RankTable` | 排行表（可切区间/正序倒序） | 股息率、概念、YTD 等排行型 |
| `DrillLink` | 下钻按钮组（K线/概念/个股跳转） | 明细区统一交互 |

---

## 4. 首发模块：市场风向（P0）

### 4.1 数据层：物化表 `market_style_daily`（每日一行）

计算任务 `market_style_sync` cron **`30 21 * * 0-4`（每工作日 21:30，仅兜底）**，纯库内 SQL 计算、幂等重建当日行。
**主触发已改为链式**：`stock_daily_incr` 成功后由调度器自动接力（`scheduler.CHAIN_NEXT`），固定时刻仅在当日链式未跑成时才执行。

> ⚠️ **排期契约（2026-09-17 二次修正，血泪教训）**：本表的输入是**个股日线**，而 `stock_daily_incr`
> （19:20 起跑）常态耗时 **15~52 分钟** —— 因此**任何早于「日线完成」的固定时刻都会读到半量日线**。
> 排期演进：`18:45`（挂指数同步后）→ `20:05` → **链式触发（固定时刻降为兜底）**。
> 2026-09-16 实测：`market_style_daily[9/16]` 的宽度类字段全 NULL → `cross_checks` 的
> **`micro`（微观结构）与 `pxvol`（量价）两项当场退化为「数据缺失」**（`nodata:2`）。
> 2026-09-17 实测：20:05 班次在日线只跑了一半时「成功」执行（写 5254 行实为**上一交易日**数据），
> 证明「把固定时刻往后挪」治标不治本 —— 日线耗时本身在 15~52 分钟间波动，任何固定值都可能赌输。
> 保留**日线充分性护栏**兜底：当日行数不足上一交易日 **90%** 时，计算上界退回上一交易日
> （`_safe_max_date()`）——**宁可停在上一交易日，不写半量数据**；此时 `run_detail` 写入
> `stale_upper_bound` 显式标注，不再静默 success。
> 同源问题在 `market_current_sync` 亦有（见 §13）。

| 字段组 | 字段 | 口径 |
|---|---|---|
| 六组收益 | `ret_bench_20/60, ret_size_20/60, ret_tech_20/60, ret_sent_20/60, ret_div_20/60, ret_pol_20/60` | 组内等权 N 日收益(%) |
| 剪刀差 | `scissors_20, scissors_60` | (中证1000+中证2000 等权) − (上证50+沪深300 等权) N 日收益 |
| 风偏 | `risk_appetite_20` | 科技成长组 − 股息防守组（20 日） |
| 情绪 | `sentiment_20` | 证券公司 − 中证全指（20 日超额） |
| 政策 | `policy_excess_20` | 中证全指房地产 − 中证全指（20 日超额） |
| 大势 | `bench_pos_pct` | 中证全指在近 250 日高低区间的分位(%) |

**风险调整（v0.4 新增，2026-09-15）** —— 修正「两条腿波动率不对称」造成的尺度漂移：

| 字段组 | 字段 | 口径 |
|---|---|---|
| 剪刀差风险调整 | `scissors_adj20` | `scissors_20` ÷ 其自身近 250 日滚动标准差 |
| 风偏风险调整 | `risk_appetite_adj20` | `risk_appetite_20` ÷ 其自身近 250 日滚动标准差 |

> **为什么需要**：两条腿的波动率并不对称——实测 σ(科技成长)/σ(股息防守) 中位 **2.21 倍**，
> 于是差值波动被高波动腿主导，「同样的 −12pp」在高波动期与低波动期含义完全不同：
> 2024 初小盘股灾 diff −16.12 / σ 4.89 → adj **−3.26**（极罕见）；当前 diff −13.49 / σ 12.16 → adj **−1.12**（常规）。
> **三条口径要点**：
> ① **刻意不减滚动均值**——减了就退化成「250 日排位」，与已有的 `pct` 分位重复，且会把趋势误当偏离；
> ② σ 恒正 → `adj` 必与原差值同号（数学必然，故 DQ 用 `style_adj_sign` 作 critical 校验）；
> ③ 中性点 **0 = 两腿同收益**，±1 = 偏离自身一个典型波动单位，可跨期比较（实测全史 mean 0.084 / sd 1.095）。
>
> **与 `pct` 的分工**：`pct` 答「在近一年排第几」（纯相对排位、受区间选择影响）；
> `adj` 答「偏离自身风险尺度几个单位」（含幅度、不受「近一年恰好是牛是熊」影响）。两者并列展示、互为参照。
> 覆盖区间：`risk_appetite_adj20` 2020-04-30 起（受科创50 等新指数上市限制）、`scissors_adj20` 2014-04-30 起。

**市场宽度（v0.3 新增，2026-09-14）** —— 源 `stock_market_daily` 个股日线，**pandas 增量计算**（非 SQL 窗口函数，见 §9 性能说明）：

| 字段组 | 字段 | 口径 |
|---|---|---|
| 涨跌家数 | `breadth_total / breadth_up / breadth_down / breadth_up_ratio` | 当日有收盘价个股数及其涨跌分布；占比=上涨/总数% |
| 情绪极值 | `limit_up / limit_down` | 涨幅≥9.8%（主板）/ ≥19.8%（创业板 30x、科创板 68x，按代码前缀判定）近似口径 |
| 均线参与度 | `above_ma20_pct / above_ma60_pct` | 收盘站上各自 MA 的个股占比%；MA60 分母为有 60 日历史的个股 |
| 新高新低 | `new_high60 / new_low60 / hl_diff60` | 创 60 日新高/新低家数及其差（市场内部强弱） |
| 腾落线 | `breadth_adl` | ADL 全史累计 Σ(上涨−下跌)，**必须基于合并后完整序列累计**，保证跨块连续 |

**换手率（v0.6 新增，2026-09-15）** —— 源 `stock_market_daily.turnover_ratio`，随宽度同一次取数：

| 字段组 | 字段 | 口径 |
|---|---|---|
| 换手率中位 | `turnover_med` | 全市场个股换手率**中位数**（%）。**必须取中位数**：源列存在 max 20.27 异常值，均值被污染近 10 倍 |

> **为什么不实时查**：按 `trade_date` 查 `turnover_ratio` 是非索引列 → 随机回表，**单日 15 秒**；
> 物化后随 `market_style_daily` 一行取到（O(1)）。这正是「微观结构」印证（涨跌停 × 换手率）能上线的前提。
> ⚠️ **源列单位漂移（v0.6 修）**：`turnover_ratio` 在 **2025-09 中旬**从百分数（`2.0` = 2%）切换为小数（`0.02` = 2%），
> **相差 100 倍**。采集器按日判断口径（阈值 `TURNOVER_FRACTION_MAX=0.3`，小数口径 ×100），
> 已入库历史段用 `UPDATE … SET turnover_med = turnover_med/100 WHERE turnover_med >= 30` 修正（5019 行）。
> 修正后全史区间 **0.46% ~ 8.59%**，分布合理；DQ 加 `style_turnover_range`（critical）兼作单位漂移守护。

**量能（v0.3 新增，2026-09-14）** —— 与宽度同源于同一次取数（零额外成本）：

| 字段组 | 字段 | 口径 |
|---|---|---|
| 成交额 | `market_amount` | 全市场个股 `amount` 求和（亿元） |
| 放缩量 | `amount_ratio_20` | 成交额 / 近 20 日均值 × 100%（>100 放量，<100 缩量） |

> 好处：口径版本化可回测、前端 O(1)、可加 DQ 规则（freshness + 行数 + 宽度非空/越界/自洽）、未来直接喂 AI 生成日报。
> 取数依赖覆盖索引 `idx_breadth_cover (stock_code, trade_date, close, change_pct)`，EXPLAIN 走 `Using index for skip scan` 全索引覆盖免回表；由 `scripts/seed_indexes.py` 创建（索引唯一事实来源）。

### 4.2 视图层（AnalysisShell 拼装）

- ③ 结论区 KPI：`风偏分数`（正=进攻/负=防守，金色高亮极值）+ `大小盘剪刀差` + `大势位置`（分位%）+ **`情绪温度` / `政策超额`**（v0.3 补白算指标）；每张 KPI 附**近一年分位**与 **z-score**（偏离均值几个标准差），跨期可比
  - **风险调整读数**（v0.4 新增）：`风偏分数` 与 `大小盘剪刀差` 两张卡额外展示一行 `风险调整 −0.99σ`
    （次要文字色 `#6B7280`，**不用红绿**——非涨跌语义）；卡片 tooltip 附口径说明，
    说明文本由后端 `adj_note` 随响应下发、前端**只透传不手抄**（避免前后端口径各写一份而漂移）
- ④ 主视图：
  - `GroupHeatBars`（六组 20 日收益热力条，**点击下钻**到该组明细）+ `DualLineTrend`（剪刀差 & 风偏分数双线，近 1 年，**叠加轮动区间底色带**：小盘占优=金、大盘占优=蓝，过滤 <3 天的翻转噪声）
  - **`BreadthPanel`**（v0.3 新增）：涨跌分布条 + 四格明细（涨停/跌停、站上 MA20%、站上 MA60%、60 日新高−新低）+ 腾落线
  - **量能条**（v0.3 新增）：全市场成交额 + 放缩量 ratio（>100 放量红、<100 缩量绿，仅数字语义用红绿）
  - **`HeatMatrix`**（v0.3 新增）：六组 × 时间（12 段等分回看窗口）热力矩阵，行点击下钻
  - **`CrossCheckPanel`**（v0.6 新增）：**第四类参照系 ④ 交叉印证** 专用面板，6 张卡片（杠杆 / 股债 / 股商 / 口径互证 / 微观结构 / 量价）。
    每卡固定四段：① 维度对（两个独立维度各自的值 / 20 日变化 / 分位 / 截至日）② 判定徽标（一致 / 背离 / 中性 / 缺失）
    ③ 读法（后端下发，前端不自己编）④ 证据（相关系数、样本数、分位等可复核统计量）。
    **配色纪律**：背离用**琥珀 `#B45309`** —— 背离既不是涨跌语义（不用红绿），也不是错误（失败才用深红棕 `#791F1F`），
    同时必须与「金色 = 系统亮点/AI 洞察」区分开，否则金色会泛滥；一致用主色蓝 `#185FA5`。
- ⑤ 明细区：六组内 21 只指数列表（最新涨跌/20 日收益/近一年分位），行内下钻 K 线；支持按组**过滤下钻**（`NTabs`）
- ⑥ **历史回放**（v0.3 新增）：`NDatePicker` 选 as_of 锚点，全链路只取 `trade_date <= as_of`，回放模式不报滞后（数据天然落后）
- 附：状态词规则（首版简单化）——风偏分数 >0 且近 5 日走扩 =「风偏升温」；<0 且收敛 =「防守松动」；其余 =「均衡震荡」
- 配色纪律（「蓝骨金魂」）：涨=红 `#EF232A` / 跌=绿 `#14B143` **仅用于行情语义数字**；占比/健康度=主色 `#185FA5`；金色 `#C9A227` 仅用于极值标记（≤10%/≥90% 分位）；区间底色带用金/蓝（表风格占优方，非涨跌）

---

## 5. 旧模块处置

- 股息率排行 / 概念排名 / YTD **从导航下线**（代码保留不删）；
- 新框架稳定后按本规范回迁：排行型用 `RankTable` 视图归入「个股基本面/板块与概念」分组，口径说明补齐。

## 6. 演进路线

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | analysis_registry（先配置文件版）+ AnalysisShell 容器 + 总览页 + 6 个内置视图组件 + 市场风向模块（含 market_style_daily + 计算任务 18:45 + API） | ✅ 2026-09-13（§8） |
| P1 | 旧 3 个分析按新框架回迁（RankTable 化） | 未开始 |
| P2 | 每日"市场体检"结论自动写入投资日历（AI 短评，金色徽章）；跟踪清单类模块（自选组合监控） | 未开始 |

> ⚠️ **两套编号并存，勿混**：本节 P0/P1/P2 是**框架演进**阶段（按 UI 框架排）；
> §11 蓝图报告的 P1/P2 是**数据参照系**阶段（按数据可得性排，P1 = 零采集成本、P2 = 需新采集）。
> 2026-09-15 落地的是**后者 P1**（见 §12，补 ④交叉印证 + 板块轮动模块），本节 P1（旧分析回迁）仍未开始。

---

## 7. 决策点（2026-09-13 用户确认，已随 P0 落地）

1. 总览页形态：**卡片墙**（跟踪型摘要卡 + 研究型目录）✅
2. 旧分析：**下线保留代码**（API 路由与 AnalysisView.vue 保留、导航摘除，框架稳定后按本规范回迁）✅
3. 注册机制：**配置文件先行**（`app/analysis/registry.py`，模块多了再落 `analysis_registry` 表）✅

## 8. P0 落地记录（2026-09-13）

| 项 | 内容 |
|---|---|
| 数据层 | `market_style_daily` 物化表（5,250 行，2005-02-01~，19 个指标列）；计算任务 `market_style_sync` cron `45 18 * * 0-4`（task_config 28→**29** 条） |
| DQ | `style_freshness` / `style_rows` 两条规则（69→**71** 条） |
| 后端 | `app/analysis/registry.py`（注册表）+ `market_wind.py`（装配）；`/api/analysis/registry` + `/api/analysis/market-wind` |
| 前端 | 总览页 `AnalysisOverview.vue`（卡片墙+目录）+ 容器 `AnalysisModuleView.vue`（/analysis/:moduleId）+ 模块 `analysis/MarketWindView.vue` + 5 个内置视图组件（KpiCards/GroupHeatBars/SizeGradient/DualLineTrend/RankTable）+ DrillLink 下钻（/market?index= 预选联动） |
| 首跑实测 | as_of 2026-09-11：风偏分数 **-8.45pp 防守松动**（科技成长 20 日 -9.19% vs 股息防守 -0.73%）、剪刀差 -0.91 大盘占优、大势位置 22.5% 偏低 |

## 9. 市场风向模块增强记录（2026-09-14，四批）

> 缘起：用户就"市场风向"主题征集优化建议，共列 16 项分四批全选落地。
> 原则：**不牺牲 P0 的"口径版本化可回测"**——所有新增指标一律物化落表，前端只读不算。

| 批次 | 主题 | 落地内容 |
|---|---|---|
| **1** | KPI 扩充与交互 | 补 `sentiment_20` / `policy_excess_20` 两个「白算」指标入 KPI；每张 KPI 加**近一年分位**（金色标记 ≤10% / ≥90% 极值）与 **z-score**；加**滞后提示**（数据落后交易日数）；KPI 卡可**锚点下钻** |
| **2** | 市场宽度 | 物化表加 **12 列**：`breadth_total/up/down/up_ratio/adl`、`limit_up/down`、`above_ma20_pct/above_ma60_pct`、`new_high60/new_low60/hl_diff60`；新增 `BreadthPanel.vue` 视图 |
| **3** | 量能 + 标准化 | 物化表加 **2 列**：`market_amount`、`amount_ratio_20`（放缩量）；`_standardize()` 对 6 组收益/KPI 统一算近 250 日分位 + z-score；新增量能条 |
| **4** | 结构性增强 | `HeatMatrix.vue`（六组×12 段热力矩阵）；`DualLineTrend` 加轮动**区间底色带**（`_sign_bands` 过滤 <3 天翻转）；`NDatePicker` **历史回放**（全链路 `as_of`）；点热力条/矩阵行 → 明细 `NTabs` **过滤下钻链** |

**性能决策（重要，勿回退）**：宽度/量能**不得**用 SQL 窗口函数——`stock_market_daily`（1813 万行/2.2GB）上 3→4 个窗口耗时从 8.2s 跳到 62.1s（会话缓冲 `tmp_table_size` 33MB 导致窗口结果集落盘）。改用 **pandas 增量**（6.5s 抓数 + 3.3s 计算 ≈ 10s/日），全史回填分块（2 年/块）+ 每块 `del raw; gc.collect()` 防内存劣化，首次全史约 3~5 分钟。

**字段口径**：宽度与量能同源同一次取数（`_fetch_raw` 多带 `amount` 列），`MARKET_COLS = BREADTH_COLS + VOLUME_COLS` 一次合并一次写入。

**DQ**：新增 3 条规则 `style_breadth_notnull`（宽度列非空率）/ `style_breadth_range`（占比越界）/ `style_breadth_hl`（`hl_diff60` 自洽）——须以种子脚本 `seed_dq_rules.py` 为准。

**已知约束**：表刚加列时存在"有行但宽度全 NULL"的过渡态，增量门控须过滤 `breadth_up_ratio IS NOT NULL`，新增派生列后以 `full_refresh=True` 强制全量重算（否则历史永远空）。

## 10. 风险调整增强（2026-09-15）

> 缘起：用户就「风偏分数」提出三个疑问（想反映什么 / 算法是否合理 / 单位 pp 何意）。
> 复核后确认原口径站得住（做差剥离 beta、组内等权要求全员有值、分位补救跨期不可比），
> 但识别出一处**真实缺陷**——两条腿波动率不对称导致差值尺度漂移。

### 10.1 问题：为什么需要风险调整

`风偏分数 = ret_tech_20 − ret_div_20` 是两条腿的收益差，但两条腿的波动率并不对等：

| 窗口 | σ(科技成长) 均值 | σ(股息防守) 均值 | σ 比（中位） |
|---|---|---|---|
| 120 日 | 8.25 | 4.52 | 2.28 |
| **250 日（采纳）** | **8.88** | **4.92** | **2.21** |
| 500 日 | 9.30 | 5.34 | 2.18 |

差值波动因此被高波动的科技端主导，「同样是 −12pp」在不同波动环境下并不可比：

| 时期 | 原始差值 | σ_diff | 风险调整后 |
|---|---|---|---|
| 2024 初小盘股灾 | −16.12 | 4.89 | **−3.26**（极罕见） |
| 2026-09 当前 | −13.49 | 12.16 | **−1.12**（常规） |

原始值看似相当，**风险调整后信号强度差约 3 倍**——低波动期的小差价反而是更强的信号。

### 10.2 口径定稿

`adj = 差值 ÷ 该差值近 250 日滚动标准差`（`min_periods=60`，与 `pct` 窗口一致）

**被否掉的两个候选方案**（记录以免日后重走弯路）：

| 方案 | 定义 | 否决原因 |
|---|---|---|
| A | `(diff − rolling_mean(diff)) / std(diff)` | 减均值引入「近一年是牛是熊」的依赖，退化成 250 日排位，与 `pct` 分位重复 |
| B | `tech/σ_tech − div/σ_div` | 中性点不是 0：两腿同涨 10% 时科技因波动大反算出负值，变成「谁夏普高」而非「资金偏好」 |

**定稿方案**（`diff / σ_diff`，不减均值）通过两项检验：中性性（两腿同收益 → 0.00）、
分布（全史 `mean 0.084 / sd 1.095`，理想归一化应接近 0 与 1；剪刀差同法处理后 `mean −0.012 / sd 1.097`）。

### 10.3 落地清单

| 层 | 内容 |
|---|---|
| 数据层 | `market_style_daily` 加 **2 列**：`risk_appetite_adj20` / `scissors_adj20`（由 `_ensure_columns` 自动 ALTER 补齐） |
| 采集器 | `market_style_sync._risk_adj()`；常量 `STYLE_ADJ_COLS` / `STYLE_ADJ_DDL` / `STYLE_ADJ_LOOKBACK=250` / `STYLE_ADJ_MIN_PERIODS=60`。新列由指数列全量重建自然带出，**无需 `full_refresh`**（该开关只管宽度列增量门控） |
| API | KPI 新增 `adj` 字段（仅风偏与剪刀差）；顶层 `adj_note` 下发口径说明供前端 tooltip 直接引用 |
| 前端 | `KpiCards.vue` 增 `adj` 行（`风险调整 −0.99σ`）+ `adjNote` prop；`MarketWindView.vue` 透传 `adj_note` |
| DQ | 加 2 条：`style_adj_sign`（符号自洽，critical）/ `style_adj_range`（\|adj\|>10，warning）；规则 76→**78** 条 |
| 验收 | 端到端独立复算（从 `dc_index_market` 重算全链路，不经 `market_style_daily` 中间值）最近 30 行**最大差 0.000000**；定向 DQ 7/7 pass |

### 10.4 与分位的分工（用户可感知的差异）

| 指标 | 回答的问题 | 特性 |
|---|---|---|
| `pct`（近 250 日分位） | 在近一年**排第几** | 纯相对排位；0~100 均匀分布；受「近一年恰好是牛是熊」影响 |
| `adj`（风险调整） | 偏离自身**风险尺度**几个单位 | 含幅度信息；不依赖区间选择；可跨期比较 |

当前实测：`−11.98pp` 对应 `pct 7.6%`（近一年极端防守）↔ `adj −0.99σ`（偏离约一个标准差）。
**两者并列展示、互为参照**——「极端」的印象来自近一年科技偏强形成的高基线，而按自身风险尺度衡量只是约一个标准差。

## 11. 数据蓝图探索（2026-09-15）

> 缘起：用户要求为分析研究确立第一原则（**数据用来比较才有意义**），并先做一轮探索——
> 不局限于现有已落地数据，摸清「做市场风向分析研究还需要哪些数据、用什么数据联合比较才有意义」。
> **本节只记录探索结论，不改变已落地口径**；完整报告见 `docs/市场风向分析数据蓝图探索报告_2026-09-15.md`。

| 项 | 结论 |
|---|---|
| 第一原则 | 确立为 **§1.0**：五类参照系（同类横比 / 自身纵比 / 基准超额 / 交叉印证 / 结构分解）+ 三类背离 + 三条硬性约束 |
| 现状覆盖度 | 现有 35 列**只覆盖①同类横比 + ⑤结构分解**两类（六组指数收益、宽度、量能），②③已有（pct/超额），**④交叉印证几乎空白** |
| 库内富矿 | 14 张表已有数据但未接入市场风向，其中 4 项**零采集成本**即可联合：两融余额（日频 2010-03~ ）、国债收益率（含中美，到 9-15）、商品期货基差（56 品种）、概念板块 K 线（379 概念） |
| 缺口实测 | 跨市场（恒生/道指/标普/纳指）、Shibor/LPR、汇率中间价、指数估值（中证官网 + 乐咕含历史分位）、新基金发行、股票回购 **源均可达** |
| 两项否决 | **北向资金日频流入**：2024-08 起交易所停止披露，接口资金列全为 NaN（不可得，勿投入）；**新增投资者**：源止于 2023-08 已停更（不可得） |
| 沿用旧结论修正 | ⚠️ **「东财全域不可达」不成立**：`push2his` 不可达，但**东财数据中心域（datacenter-web）可达**——北向历史、新基金、回购、赚钱效应等接口实测均通 |

**探索副产物：发现 7 处数据缺陷**（详见报告 §7），其中 3 处需优先处理：
`stock_market_current` 的 8 列（含 PE/PB/总市值）**全表为空**；
`securities_margin.rzrqyecz` 存值疑似口径错配（值≈余额而非占比）；
`stock_capital_flow` 与 `stock_market_daily_ex` 双双**停更于 2025-09-19**（近一年未更新）。

---

## 12. P1 落地记录：零采集成本阶段（2026-09-15）

> 承接 §11 蓝图报告 §8 的路线图。**P1 = 不新增任何采集任务**，只把库内**已有但未接入**的数据接进装配层，
> 目标是把 §1.0 里唯一整类空白的 **④交叉印证** 补上（背离信号全部来自这一类），
> 同时把「检验 ④ 是否可靠」所需的**第二套独立口径**（板块轮动）建起来。P2（需新采集）暂缓。

### 12.1 判定方法：方向背离法（不做加权打分）

§1.0 说了「背离是最高级用法」，但没说怎么判。P1 定的方法刻意**可解释、可复核**：

| 步骤 | 规则 |
|---|---|
| ① 取变化 | 每个维度取**自身 20 日变化**（不是水平值——水平值高低不可比） |
| ② 定死区 | 死区 = **该维度自身近一年 20 日变化幅度的中位数 × 0.5**（`DEAD_RATIO`），低于死区视为「噪声不动」 |
| ③ 判方向 | 两维方向**相反且都超出各自死区** → `diverge`；同向 → `agree`；任一在死区内 → `neutral`；无料 → `nodata` |

> **为什么不用打分**：打分说不清分数怎么来的，方向背离可解释、可复核，且天然适配「维度数量会继续增加」；
> 每个维度**用自己的尺度做死区**，避免了「国债收益率变化 1bp」与「两融变化 2.63 万亿」直接比较的荒谬。
> 常量：`WINDOW=250`、`CHG_LAG=20`、`DEAD_RATIO=0.5`；实现见 `backend/app/analysis/cross_check.py`。

### 12.2 六维交叉印证（`cross_check.py`）

| key | 维度对 | 外部参照物来源 | 实测（截至 2026-09-15） |
|---|---|---|---|
| `leverage` | 风偏分数 × 两融余额 | `securities_margin`（日频 2010-03~） | **一致**（同向收缩）但力度不对称 2.3× vs 0.9× |
| `bond` | 大势位置 × 10Y 国债收益率 | `bond_profit_daily.cn_bond_10y` | **背离**：利率 8.1% 分位（距区间最低 1bp）而股指 18.7% 分位 |
| `commodity` | 周期风格组 × 商品基差偏离 | `futures_spot_price`（56 品种，基差 180 日均） | **背离**：新能源链基差 +0.95z 升水，科技成长组 20 日 −13.24% |
| `concept` | 六组等权中位 × 概念中位 | `ths_concept_market`（375 概念） | **一致**：−5.15% vs −3.89%，两套独立数据集互证 |
| `micro` | 涨跌停家数 × 换手率中位 | `market_style_daily.limit_up/down` + `turnover_med` | **背离**：跌停 35 > 涨停 31，但换手率仅 2.4% 分位（缩量） |
| `pxvol` | 大盘 20 日收益 × 量比 | `market_style_daily.ret_bench_20` + `amount_ratio_20` | **背离**：大盘 −7.57% 而量比 83%（缩量下跌） |

**商品分组（`GOOD_GROUPS`）**：新能源链（多晶硅 / 碳酸锂 / 工业硅 → 科技成长组）、黑色建材链（→ 政策周期组）。
每个品种的基差偏离先除以**自身近一年偏离的标准差**得 z，再取全品种中位 —— 口径与 §4.1 的「风险调整」同源。

**诚实标注的缺口**：库内**没有指数估值分母**（`stock_market_current` 的 PE/PB 等 8 列全表为空），
故本模块**不做 ERP（股权风险溢价）**，股债项只做「利率水平 × 股指位置」的方向性判读；
接入指数估值后应升级为 ERP 分位。`FRAMEWORK_NOTE` 随响应下发（`cross_note`），前端只透传。

### 12.3 新模块「板块轮动」（`sector_rotation.py` + `/analysis/sector-rotation`）

落 **①同类横比 + ③基准超额**，末尾做 **④双侧口径互证**：

| 项 | 口径 |
|---|---|
| 行业口径 | `stock_industry_sw`（**申万一级 31 / 二级 131**）× `stock_market_daily` → 行业内个股**等权** 20 日收益 |
| 相对超额 | 行业 20 日收益 − `market_style_daily.ret_bench_20`（③：普跌日里 −2% 的行业其实是跑赢） |
| 轮动速度 | 各行业 20 日收益的**离散度**（标准差）+ 首尾差 pp —— 单看排行看不出「齐涨齐跌 vs 剧烈轮动」 |
| 概念口径 | `ths_concept_market`，每概念自身最近 21 个交易日收益，锚定「覆盖数 ≥300」的完整日 |
| 互证 | 两个**完全独立**的数据集（申万分类+个股等权 vs 同花顺概念指数）同向 = 互证成立；打架本身是信息 |

**三个实现要点**：
1. ⚠️ **必须按 `stock_code` 顺序取数** —— `stock_market_daily` 按 (stock_code, trade_date) 聚集，按 `trade_date` 查是大范围随机回表。
2. ⚠️ `stock_industry_sw.industry_type` 取值是「**申万一级 / 申万二级**」（不是「一级行业」），且一级与二级**混存同一列** `industry_name` —— 不过滤会把 131 个二级行业混进一级排行。
3. ⚠️ **概念源分批发布**：收盘后一段时间只有 ~190/375 个概念，须锚定覆盖完整的交易日；并剔除 |收益|>60% 的异常样本。

**性能**：行业排行需扫个股日线快照（首跑 ~9~17s）→ 引入 `backend/app/analysis/_cache.py`（进程内 TTL 缓存，线程安全），
`@ttl_cache(600)`。二次命中缓存 0.0s。**适用边界**：日频、盘后更新的分析视图；数据更新后最长 TTL 秒内仍返回旧值。

### 12.4 顺带修掉的三个缺陷

| # | 缺陷 | 修法 |
|---|---|---|
| 1 | **换手率源列单位漂移**：`turnover_ratio` 于 2025-09 中旬由百分数改小数，差 100 倍（首版物化×100 致历史段虚高 100 倍，max 859%） | 采集器按日判口径（`TURNOVER_FRACTION_MAX=0.3`）；已入库 5019 行 ÷100；三段复算偏差全 0.000000 |
| 2 | **模块间切换时头部元数据残留**：`AnalysisModuleView` 对 `/analysis/:moduleId` 是同一路由记录，vue-router **复用实例不重新挂载** → `onMounted` 不重跑 → 头部名称/口径/分组残留上一个模块（截图实测发现） | `onMounted` → `watch(moduleId, …, { immediate: true })` |
| 3 | **`**强调**` 标记被原样显示**：口径文案由后端下发且带 markdown 强调，前端插值直出 → 可见 `**互证成立**` 字样 | 新增极简 `RichText.vue`（**只支持 `**粗体**`**，不引完整 markdown 库以免扩大 XSS 面），接入 `CrossCheckPanel` / `SectorRotationView` / `AnalysisModuleView` / `AnalysisOverview` |

> 第 3 条是「口径随响应下发」（§1.1 约束 2）的**必然配套**：既然后端下发富文本口径，前端就得有一层最小的渲染约定。

### 12.5 验收证据（2026-09-15）

| 项 | 结果 |
|---|---|
| API | `/api/analysis/market-wind` 含 `cross_checks`(6) / `cross_summary` / `cross_note`，1.9s；`/api/analysis/sector-rotation?level=一级\|二级` 正常，首跑 1.5s / 缓存 0.0s |
| 交叉印证逻辑正确性 | 历史回放 2024-02-05（小盘股灾）= 4 一致 2 背离；当前 = 2 一致 4 背离（方向判定随市况变化，非恒定输出） |
| 数据一致性 | `turnover_med` 独立复算偏差 0.000000；分段复算（修正前后三段）全 0.000000 |
| DQ | `market_style_daily` 规则 7 → **9 条**（新增 `style_turnover_notnull` warning / `style_turnover_range` critical），全库 **80 条**，定向运行 9/9 pass |
| 前端 | `vue-tsc -b --force` 0 error；puppeteer 实测 overview / 市场风向 / 板块轮动 三页 **0 console error / 0 warning**、可见文本 markdown 残留 **0**、横向溢出 **0**（排除固定侧栏） |

### 12.6 后续（P2 起，需新采集）

按 §11 报告 §8 路线图，P2 增量为 **跨市场（恒生/道指/标普/纳指）+ 汇率 + Shibor/LPR + 指数估值（含历史分位）**，
其中**指数估值**是解锁 ERP 的前置（见 §12.2 缺口）；
**北向资金日频**与**新增投资者**两项已在 §11 判定为源不可得，**不再列入路线图**。

---

## 13. 盘后排期契约订正（2026-09-16）

> 缘起：P1 交付次日例行巡检时，发现「市场风向」的交叉印证退化成 `nodata`，
> 顺链查出**两个比 P1 本身更严重的生产缺陷**。本节记录根因、处置与验收，供后续任何
> 「以个股日线为输入」的派生任务参照。

### 13.1 缺陷一：调度器「进程存活但调度线程卡死」

| 观感 | 实际 |
|---|---|
| `/api/health` 正常返回 | 进程确实活着 |
| `scheduler_running=true`、`scheduled_count=26` | **接口认为一切正常** |
| —— | **11.5 小时零任务派发**（07:31 停摆；`news_fetch` 是 30 分钟班次，应有 ~23 条记录，实际 0 条） |

**唯一可靠判据**：`GET /api/jobs/tasks` 的 **`next_run`** 全部停在**过去**。
⚠️ 字段名是 `next_run`，**不是** `next_run_time` —— 差几个字，探针会误判「调度器已死」。

**诱因**：疑似执行器线程池被极端耗时任务耗尽。近 7 天多次出现同类任务：
`financial_abstract_sync` 实测跑 **11 小时 51 分**，`sw_industry_sync` 最长 **21.9 小时**。

**处置**：重启即可自愈 —— `reap_orphan_runs()` 清僵尸 + `catchup_missed()` 自动补跑遗漏班次。

### 13.2 缺陷二：快照 × 日线排期竞态是**结构性**的，不是偶发

`stock_daily_incr` 实测常态耗时 **15~50 分钟**（9/11=1505s、9/14=2560s、9/15=938s），
而原排期是 `market_style_sync` 18:45、`market_current_sync` 19:30 —— **正常交易日里两者必然早于日线完成**。

**实证**：9/16 日线 19:04 开跑，物化表 19:04:54–19:07:35 就 success 了 → `market_style_daily[9/16]`
宽度类字段全 NULL → 交叉印证 `micro` / `pxvol` 当场退化为「数据缺失」。

**连锁伤害（新查出）**：

```
9/15 19:30 快照在日线未完成时触发 → 只写 2820 行（当日最终 5119 行）
        ↓
9/15 19:52 起 stock_info_sync 连续失败 6 次，错误恒为「本地快照仅 2820 只，不可作全市场名单」
        ↓
A 股在册名单停更 6 个班次
```

`stock_info_sync` 自身护栏是对的（拒绝用残数据污染名单），**错的是上游喂了残数据** ——
但下游把「上游残缺」报成了「自身失败」，把真实根因掩盖了。

### 13.3 处置

**① 排期后移**（`PUT /api/jobs/tasks/{name}` 会热同步调度器，**改完即时生效、无需重启**）：

| 任务 | 原 | 新 | 依据 |
|---|---|---|---|
| `market_style_sync` | 18:45 | **20:05** | 输入为个股日线，18:45 必然早于日线完成 |
| `market_current_sync` | 19:30 | **20:15** | 同上；且残快照会毒害下游名单任务 |
| `index_market_sync` | 18:30 | **不动** | 只输出指数行情，不依赖个股日线 |

新链条：`19:00 日线 → 20:05 物化表 → 20:15 快照 → 20:30 体检`。
已回写 `seed_task_config.py`，并同步 `seed_market_style.py`（后者一度仍是 18:45，重跑会把排期改回去 —— 已对齐并标注权威来源）。

**② 双重充分性护栏**（`MIN_ROWS=1000` 只防「表被清空」，拦不住半量数据）：

- `market_current_sync.py`：不足上一交易日 **`PREV_RATIO=90%`** 即**拒绝覆盖**，保留库内原有完整快照（宁可晚一天，不毒害下游）；
- `market_style_sync.py`：新增 `_safe_max_date()`，计算前把上界**退回上一交易日**（不足 `DAILY_SUFFICIENT_RATIO=90%` 时）。

实时验证（日线跑到 2655/5119 行时）：
快照 —— 绝对护栏**通过**（2655>1000）、充分性护栏**⛔拒绝**（正确命中旧护栏盲区）；
物化表 —— `max_d 9/16` → 护栏后**退回 9/15** ✅。

### 13.4 验收（9/16 全链路）

| 项 | 结果 |
|---|---|
| 日线 | 19:56 收尾，**5119 行**（与 9/15 持平） |
| 物化表 | 20:05 班次**自然触发**成功（5253 行）→ `ret_size_20` / `scissors_20` 由 NULL 补齐 |
| 快照 | 5119 行（原 2820） |
| 依赖链自愈 | 重启后 `catchup_missed` 自动补跑 `stock_info_sync` → **5119 只在名单、成功**（原连续失败 6 次） |
| 交叉印证 | `nodata:2` → **`nodata:0`**；`micro` 恢复为「涨停 90 > 跌停 4 但换手率仅 3.2% 分位 = 缩量上涨」、`pxvol` 恢复为「大盘 -2.50% 且量比 96% = 量价配合」 |

### 13.5 遗留（未改，待观察）

指数行情存在**源端发布滞后**：9/16 首次同步只有 19/21 个指数，缺 **中证500（000905）** 与
**中证2000（932000）**（中证官网晚发），导致 `ret_size_20` 一度为 NULL。
`index_market_sync` 的 `overlap_days=7` 会在下次运行自动补齐（本次手动触发即已补齐、21/21）——
**属设计内的自愈行为，不需要额外改动**，但意味着「当日 20:05 跑物化表时个别指数可能仍缺」，
表现为市值风格/剪刀差当日为 NULL、次日自动转好。

---

## 14. 指标口径说明入口（2026-09-19，落地 §1.1 第 4 条）

### 14.1 缺口：口径文案早就有了，只是卡片墙没接

用户反馈「大势位置、风偏分数这些，能不能加个说明，鼠标悬浮时弹窗讲清含义」。核查后发现
**这不是新功能，是既有规范在总览页漏接**：

| 检查项 | 实测（2026-09-19） |
|---|---|
| 后端 KPI 是否带 `hint` | **28/28 全覆盖**（最长 243 字 ERP，最短 32 字，7 条含 `**强调**`） |
| 详情页是否消费 | ✅ `KpiCards.vue` 早已渲染 —— 但用 `{{ k.hint }}` 直出，`**` 原样显示（既有缺陷） |
| 总览页卡片墙是否消费 | ❌ `AnalysisOverview.vue` 的 `CardKpi` 接口**未声明 `hint`**，模板里也没有入口 |
| 规范是否已要求 | ✅ 本规范 §1.1 第 4 条「每个指标旁边可查计算口径」 |

> **关键点**：`pickCardKpis()` 只做 filter / sort / slice、**不剥字段** —— 运行时 `cardKpis`
> 里每个对象其实一直带着 `hint`。所以这是**纯前端接线**：零后端改动、零数据改动、零采集改动。
> 找缺口时「接口 200 + 组件渲染了」不等于「要素齐了」，要逐要素校验（本项目已把这条写进技能）。

### 14.2 交互规格（设计原型 `docs/design/指标口径说明_交互原型_v0.1.html`，用户 2026-09-19 定稿）

| 决策点 | 定稿 | 理由 |
|---|---|---|
| 图标形态 | **ⓘ 描边款**（描边 `#8D97A5`） | 信息类通用符号、语义最准。相比「?」不暗示「求助」；相比「!」不与项目已占用的琥珀警告色（数据延迟 / cron 无效）撞义。⚠️ 描边不能取 `#B8BEC9` —— 实测该色在 14px 下过淡，缩略图里几乎不可见 |
| 显示时机 | 卡片墙**悬停显形**；详情页**常显** | 卡片墙信息密集，18 个图标常显会与数字抢注意力；详情页卡片大、信息少，常显更易发现。触摸设备（`@media (hover: none)`）无 hover → 一律常显，否则等于没有入口 |
| 触发方式 | 悬浮，**延迟 150ms** | 延迟避免鼠标扫过时「乱闪」 |
| 浮窗风格 | **白底信息卡**（白底 + 8px 圆角 + 轻阴影） | naive-ui Tooltip 默认深色；本项目口径最长 243 字，深色大段正文可读性明显劣于白底（原型里两版实测对比过） |
| 覆盖范围 | 卡片墙 + 详情页**一并统一** | 两页共用同一个 `KpiHint.vue`，避免同一指标在两处口径外观不一致 |
| `hint` 文本形态 | 先**直接渲染现有文本**（路线 A） | 现有 `hint` 里混有面向开发的口径备注（如「不能用最新公告日统计…」），结构化成「是什么·怎么算·怎么读·备注」属**口径治理**这个更上游的议题，不与之捆绑；留作后续独立小批次 |

### 14.3 组件契约：`frontend/src/components/analysis/KpiHint.vue`

props：`label`（浮窗标题 = 指标名）/ `hint`（正文）/ `value`（当前值）/ `note`（附注，详情页传 `ADJ_NOTE`）/ `placement`。

**触发元素由调用方通过 `#trigger` slot 决定**（两页恰好不同），**浮窗渲染全局只有一份**：

- 卡片墙 → 触发元素 = ⓘ 图标本身：卡片是 `@click=open(m)` 的跳转按钮，整卡悬浮会与「点击进详情」互相干扰；
- 详情页 → 触发元素 = 整张卡片：卡片不可跳转，整卡悬浮更顺手，也保留了既有习惯。

三条硬约束（改动前必读）：

1. **`hint` 必须走 `RichText` 渲染** —— 后端文案用 `**…**` 标重点，`{{ hint }}` 会把 `**` 原样显示。
2. **卡片墙的 ⓘ 必须 `@click.stop`** —— 否则冒泡到卡片，用户「想看口径」却「被跳进详情页」。
3. **浮窗不要加 1px 描边** —— naive-ui 的箭头是不带边框的旋转方块，用 `box-shadow` 模拟描边会让箭头根部缺一条线；只用阴影更干净。

### 14.4 验收（2026-09-19 实测，puppeteer DOM 量测，非目测）

| 断言 | 实测 |
|---|---|
| 卡片墙 KPI 的 ⓘ 覆盖率 | **18/18**（5 张卡：整行卡 6 + 半宽卡 3×4） |
| 悬停显形是否按卡片生效 | 被悬停卡图标 `opacity=1`，其余卡 `0` |
| 浮窗背景 / 圆角 | `rgb(255,255,255)` / `8px` |
| 浮窗尺寸 | 344×249px（243 字 ERP 完整容纳，未横贯屏幕） |
| `**强调**` 渲染 | `<strong>` 内容 =「近 250 个月末」，无 `**` 残留 |
| 点击 ⓘ 是否跳页 | URL 不变（`/#/analysis` → `/#/analysis`） |
| 详情页圆标 | 已统一为 ⓘ 描边；`**` 直出缺陷已修（`rawMark = false`） |
| console / pageerror | **0 条** |

### 14.5 已知留白

- ⓘ 带 `tabindex="0"` 与 `aria-label`（语义可达），但弹窗当前**只响应 hover**，未做 focus 触发 ——
  键盘用户能聚焦、看不到浮窗。原型演示过 focus 触发，本轮未实现，属已知留白而非缺陷。
- 浮窗底部固定落款「口径由后端随响应下发，前端只透传」：面向审计者说明文案来源，
  避免读者怀疑是前端写死的。若后续认为对终端用户冗余，可改为调试开关控制。


