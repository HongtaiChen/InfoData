# InvestBuddy 数据体系设计规范 v1.5

> v1.5（2026-09-05）：**stock_info 列序重排 + data_source 语义改造**（用户决策：保留列并改为三源构成）。合并宽表后 `data_source` 全表唯一常量 `'ADTA'` 已无法反映真实来源（东财名单/Baostock 状态/巨潮档案三采集器共同维护），故：值统一为 `'EM;BAOSTOCK;CNINFO'` 并设列 DEFAULT（新上市走默认值，采集器 INSERT 不再写入）；列移至表尾管理列区；`update_time` 注释澄清为「最近维护时间」（ON UPDATE CURRENT_TIMESTAMP，任一采集器刷新即更新）；**列序整体重排为 证券核心(1-7) → 巨潮档案 24 列 → 管理列收尾(profile_updated_at/update_time/data_source)**；表注释改为三源构成口径。列级细分来源由各列注释表达（Baostock/巨潮已标注）。新增可复现脚本 `scripts/reorder_stock_info.py`。表 29、任务 15、规则 31 不变。
> v1.4（2026-09-05）：**档案表合并**。`stock_company_profile` 与 `stock_info` 物理合并为一张宽表（用户决策）——档案 24 列 + `profile_updated_at` 并入 `stock_info`（34 列）；巨潮采集器改为列级 UPDATE stock_info（不触碰证券列），`list_date` 以推断口径为唯一权威（巨潮对整体上市公司口径不同弃用）；旧表改名 `stock_company_profile_bak_20260905` 留回滚点；DQ 增 `where_count` 检查器（静态表任意条件计数）+ 3 条退市一致性/档案覆盖规则（28→31 条）。表 30→29、任务 15 不变（写表目标调整）。
> v1.3（2026-09-05）：**股票档案域落地**。`stock_info` 增补 `list_status`/`delist_date`（Baostock 上市/退市状态，回填 307 只缺上市日期中的 231 只，退市 331 只带官方退市日）；新增 `stock_company_profile` 公司档案宽表（巨潮官方源 26 字段）与 `stock_company_sync`/`stock_status_sync` 两个采集任务（任务 13→15、表 29→30）。
> v1.2（2026-09-05）：**数据中心叠加质量结果 + 文档补齐**。新增 `/api/dq/table-status` 按表聚合端点；数据中心左侧每行质量状态点（绿/黄/红/灰）+ 右侧选中表质量摘要块 + 跳「数据质量」栏目闭环；新增 §10.3 DQ 规则集清单（修复 §5.3 悬空引用）；表数量 27→29、任务 12→13、健康结论同步刷新。
> v1.1（2026-09-05）：**DQ 自动化体检落地**。新增 dq_rules（28 条规则配置）+ dq_report（每日体检结果）+ data_quality_check 任务（每日 20:30 自动跑）+ 前端「数据质量」栏目。
> §5.1 / §5.3 / §9 对应章节同步刷新；其余增量沿用 v1.0 盘点结论。

> **文档定位**：本规范是 InvestBuddy 的两大设计资产之一（与 `颜色体系设计规范.md` 同级）。
> 它回答三个问题：**平台有哪些数据、数据从哪来、数据健康吗**，并为后续优化提供可勾选的路线图。
>
> **盘点基线**：2026-09-05（库表/行数/新鲜度为当日实测；作业配置取自 `task_config` 实时快照）
> **适用范围**：InvestBuddy 全栈（采集器 / 数据库 / 分析模块 / 前端栏目）；同时作为个人金融数据仓库的可复用方法论
> **维护约定**：每次「新增表 / 新增采集器 / 数据源变更 / 健康度重大变化」必须同步更新本文档对应章节；建议每季度做一次全量健康盘点并刷新 §5 快照。

---

## 1. 数据体系总览

### 1.1 使命

> **把 A 股市场的公开数据，沉淀为一份本地私有、可持续更新、可交叉分析、可 AI 增强的数据资产。**

与颜色体系的"品牌一致性"定位不同，数据体系是**平台价值的底座**：前端七个栏目的每一个画面，最终都来自这里某几张表。

### 1.2 分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│  展示层  Frontend（七个栏目 + 数据中心只读浏览器）                │
│  行情看板 · 概念 · 日历 · 资讯 · 分析 · 作业监控 · 系统设置 · 数据中心 │
└───────────────▲──────────────────────────▲─────────────────────┘
                │ REST /api/*（9 模块）      │ 只读 SELECT
┌───────────────┴──────────────────────────┴─────────────────────┐
│  服务层  FastAPI backend/app                                    │
│  api/ 9 模块 · analysis/ 4 个分析模板 · tasks/run.py 统一入口      │
└───────────────▲────────────────────────────────────────────────┘
                │ 写表
┌───────────────┴────────────────────────────────────────────────┐
│  采集层  backend/app/collectors（15 个已实现，2026-09-05 补齐 6 个遗留 + DQ + 档案域）  │
│  APScheduler 调度器 ← task_config 热配置 → task_runs 全程记录     │
└───────────────▲────────────────────────────────────────────────┘
                │ 拉取
┌───────────────┴────────────────────────────────────────────────┐
│  数据源  东财(日线/快讯/快照) · 腾讯/新浪(日线降级) · 同花顺(概念/分红) · Baostock(状态) · 巨潮(档案)  │
│         财联社(电报) · 豆包方舟 AI（概念×事件分析，待重新激活）        │
└────────────────────────────────────────────────────────────────┘
       存储层：本地 MySQL `adata` 库 —— 29 张表 · 约 5,300 万估算行 · ~7.5GB（另保留备份表 1 张待归档）
```

### 1.3 关键数字速览

| 维度 | 数值 | 说明 |
|---|---|---|
| 业务库 | `adata`（utf8mb4） | 本地 MySQL，1990-12-19 起 36 年跨度 |
| 表数量 | 29 张 = 25 业务 + 4 系统 | `task_config` / `task_runs` 为调度系统表；v1.2 增 `dq_rules` / `dq_report`；v1.4 档案表并入 stock_info（另 1 张备份表待归档） |
| 数据规模 | 全库估算 **~5,250 万行 / ~7.5GB** | 含备份与复权衍生；剔除备份 ≈ 3,640 万行 |
| 股票覆盖 | 5,786 只（日线库） | 沪深京 A/B，1990-12-19 上交所首日至今 |
| 核心行情表 | `stock_market_daily` 17,833,101 行 / 2.1GB | 唯一键 (stock_code, trade_date) |
| 定时作业 | 15 个配置 = **15 个全部已实现** | 2026-09-05 补齐 6 个遗留 + data_quality_check + stock_status/company_sync（见 §3.2/§3.3） |
| 质量体检 | 31 条规则 / 10 类检查器 / 每日 20:30 | 覆盖 12 张自更新表；结果实时聚合到数据中心（见 §5.1/§10.3） |
| 股票档案域（v1.3/v1.4） | `stock_info` 宽表（34 列） | 证券级 9 列 + 巨潮档案 24 列（全称/主营/经营范围等）+ profile_updated_at；Baostock 上市/退市状态（退市 331 只带官方退市日） |
| 数据源 | 7 类外部源 + 1 本地聚合 + 1 AI | 东财/腾讯/新浪/同花顺/Tushare + v1.3 Baostock(状态)/巨潮(档案)（见 §4） |
| 分析资产 | 4 个 Python 分析模板 → 3 个公开 API | ytd / dividend / concept-rank + AI |
| 同步频率 | 30 分钟 ~ 每月 | news 最密、月频任务为低频快照 |

---

## 2. 存储层：库表目录（核心章节）

### 2.1 分域视图

| 数据域 | 表 | 状态 | 说明 |
|---|---|---|---|
| **行情 · 日线** | `stock_market_daily` | 🟡 缺 09-02~04 | 全市场日 K（q fq 前复权） |
| 行情 · 快照 | `stock_market_current` | 🟢 | 由日线聚合的"最新交易日收盘快照" |
| 行情 · 复权衍生 | `stock_market_daily_ex` | 🔴 | 除权口径日线（注释缺失，口径待核） |
| 行情 · 备份 | `stock_market_daily_bak_20250802` | 💤 | 2025-08-02 备份快照，可归档 |
| 指数/商品/债 | `dc_index_market` `futures_spot_price` `bond_profit_daily` | 🔴 | 东财指数、期现、国债收益率 |
| 概念 | `ths_concept_info` `ths_concept_market` `ths_stock_concepts` | 🟢 / 🟢 / 🔴 | 同花顺概念清单/日线/成分关系 |
| 资讯 | `news` | 🟢 | 东财 em + 财联社 cls 双源 |
| 日历 | `trade_calendar` | 🟢 | 交易日历（覆盖至 2026-12-31） |
| 日历事件 | `finance_calendar` | 🟡 | 财经日历（2026-01-06 停更，源失效） |
| AI 结果 | `finance_concept_analysis` | 🟡 | AI 概念×事件分析（占位模式） |
| 资金/两融 | `stock_capital_flow` `securities_margin` | 🔴 | 日度资金流、融资融券 |
| 财务/分红 | `stock_financial_abstract_ths` `ths_stock_dividend` | 🔴 | 关键指标、分红派息 |
| 股本/行业/资料 | `stock_shares` `stock_industry_sw` | 🔴 | 股本、申万行业 |
| 股票资料/档案 | `stock_info` `stock_info_ex` | 🟢 | 证券级 + 巨潮公司档案合并宽表（v1.4，34 列）；stock_info_ex 人工标记保留 |
| 机构调研 | `stock_jgdy_detail` | 🔴 | 机构调研明细 |
| 基金 | `fund_info` `stock_hold_by_fund` | 🔴 | 基金信息、基金重仓 |
| 系统 | `task_config` `task_runs` | 🟢 | 调度配置与运行记录 |
| 质量体检（v1.1+） | `dq_rules` `dq_report` | 🟢 | 规则配置（31 条）+ 每轮体检快照，支撑「数据质量」栏目与数据中心标记 |

### 2.2 全表清单（盘点日实测）

> 行数/体积来自 `information_schema` 估算；「最新数据」取该表时间列 `MAX()`（有索引才执行，安全）。
> 状态：🟢 健康自更新 · 🟡 有缺口/降级 · 🔴 停更（死表）· 💤 可归档

| 表名 | 中文名 | 域 | 估算行数 | 体积 | 时间覆盖 | 最新数据 | 采集频率 | 数据源 | 状态 |
|---|---|---|---|---|---|---|---|---|---|
| stock_market_daily | 股票日线 | 行情 | 17,833,101 | 2,091MB | 1990-12-19~ | **2026-09-01** | 每工作日 19:00 | 东财→腾讯→新浪→Tushare | 🟡 |
| stock_market_daily_ex | 日线·除权扩展 | 行情 | 16,252,983 | 3,197MB | — | 2025-09-19 | 无任务 | 历史导入 | 🔴 |
| stock_market_daily_bak_20250802 | 日线备份 | 行情 | 16,050,232 | 1,905MB | — | 2025-07-31 | 一次性 | 备份 | 💤 |
| stock_market_current | 每日行情快照 | 行情 | 5,215 | 1.8MB | — | 2026-09-04 18:30 | 工作日 18:30 | 本地聚合 | 🟢 |
| dc_index_market | 东财指数行情 | 指数 | ~55,250 | 13.2MB | — | 2026-09-04 | 工作日 18:30 | 东财→腾讯 | 🟡 |
| futures_spot_price | 现货期货价格 | 商品 | 136,494 | 27.0MB | — | 2025-09-15 | 无任务 | 历史导入 | 🔴 |
| bond_profit_daily | 国债收益率 | 债券 | 9,336 | 1.9MB | — | 2026-09-04 | 工作日 08:00 | 中债+美债(akshare) | 🟢 |
| ths_concept_info | 同花顺概念清单 | 概念 | 406 | 0.1MB | — | 2026-09-03 | 每周一 02:00 | 同花顺(akshare) | 🟢 |
| ths_concept_market | 概念指数日线 | 概念 | 564,780 | 120.7MB | 2018-04-12~ | 2026-09-04 | 工作日 20:00 | 同花顺(akshare) | 🟢 |
| ths_stock_concepts | 股票-概念关系 | 概念 | ~58,450 | 12.6MB | — | 2026-09-05 | 每周一 02:00 | 同花顺→新浪 | 🟡 |
| news | 财经资讯 | 资讯 | 648 | 0.4MB | 2026-09-03~ | 2026-09-05 10:51 | 每 30 分钟 | 东财快讯+财联社 | 🟢 |
| trade_calendar | 交易日历 | 日历 | 7,845 | 2.3MB | — | 2026-12-31 | 每周日 02:30 | 新浪(akshare) | 🟢 |
| finance_calendar | 财经日历事件 | 日历 | ~2,071 | 1.7MB | — | **2026-09-05** | 每日 08:00 | 东财日历 EM-CAL + JY 历史 | 🟢 |
| finance_concept_analysis | AI 概念分析 | AI | 677 | 0.4MB | — | 2026-01-06 前后 | 手动（disabled） | 豆包方舟 | 🟡 |
| stock_capital_flow | 日度资金流向 | 资金 | 706,180 | 147.2MB | — | 2025-09-19 | 无任务 | 历史导入 | 🔴 |
| securities_margin | 融资融券余额 | 资金 | 3,848 | 0.5MB | — | 2025-09-18 | 无任务 | 历史导入 | 🔴 |
| stock_financial_abstract_ths | 股票关键指标 | 财务 | 322,619 | 67.6MB | — | 2025-06-30 | 无任务 | 历史导入 | 🔴 |
| ths_stock_dividend | 股票分红派息 | 财务 | 138,826 | 29.1MB | — | 2025-08-21 | 无任务 | 历史导入 | 🔴 |
| stock_shares | 股票股本信息 | 基本面 | 140,938 | 25.6MB | — | 2025-08-16 | 无任务 | 历史导入 | 🔴 |
| stock_industry_sw | 申万一二级行业 | 行业 | 6,956 | 2.0MB | — | 2025-08-16 | 无任务 | 历史导入 | 🔴 |
| stock_info | 股票信息+公司档案宽表 | 资料 | 5,856 | 1.1MB | — | 2026-09-05 | 证券周更(一02:00)+档案日更(08:30) | 东财全A + Baostock 状态 + 巨潮档案（v1.4 三源合一 34 列） | 🟢 |
| stock_info_ex | 股票信息扩展 | 资料 | 5,850 | 0.8MB | — | 2026-09-05 | 随 stock_info 周更 | 同左；人工标记保留 | 🟢 |
| ~~stock_company_profile~~ | ~~公司档案宽表~~ | — | — | — | — | — | — | **v1.4 并入 stock_info**；原表改名 `stock_company_profile_bak_20260905` 待归档 | — |
| stock_jgdy_detail | 机构调研明细 | 调研 | 22,141 | 11.5MB | — | 2025-09-20 | 无任务 | 历史导入 | 🔴 |
| fund_info | 基金基本信息 | 基金 | 27,790 | 3.7MB | — | 2026-09-05 | 每月 1 日 | 东财基金列表(akshare) | 🟢 |
| stock_hold_by_fund | 基金重仓 | 基金 | 113,017 | 17.6MB | — | 2025-08-16 | 无任务 | 历史导入 | 🔴 |
| task_config | 采集任务配置 | 系统 | 15 | — | — | 2026-09-05 | — | 手动 | 🟢 |
| task_runs | 作业运行记录 | 系统 | ~100(保留) | — | — | 2026-09-05 20:30 | 每次执行 | 自产 | 🟢 |
| dq_rules | 数据质量规则配置 | 质量体检 | 31 | — | — | 2026-09-05 | 手动维护 | 自产 | 🟢 |
| dq_report | 数据质量体检结果 | 质量体检 | ~31/轮×N | — | — | 2026-09-05 20:30 | 每日 20:30 | 自产 | 🟢 |

> **健康结论**：全库 29 张表中，**健康自更新 14 张（业务 12 + 质量体检 2）、有缺口 4 张、停更/可归档 11 张**（另保留备份表 `stock_company_profile_bak_20260905` 待归档）。
> 2026-09-05 补齐 6 个遗留采集作业后，bond/finance_calendar/fund_info/stock_info/stock_info_ex 已恢复 🟢；
> dc_index_market（13 指数已补 12，931775 待生产东财首跑）与 ths_stock_concepts（生产同花顺通道全量首跑后转 🟢）为 🟡；
> 其余 11 张死表为 2025-08/09 旧采集体系遗留——**数据仍在、价值仍在，只是"没有采集器喂养"**，属可恢复资产而非垃圾（见 §9 规划）。

### 2.3 命名与编码约定（沉淀基线，新表必须遵守）

| 约定 | 规则 | 示例 |
|---|---|---|
| 表名 | snake_case、语义域名前缀 | `stock_*` `ths_*` `trade_*` `finance_*` |
| 数据源标识 | 前缀区分来源体系 | `ths_`(同花顺) `dc_`(东财) 无前缀=主体系 |
| 股票代码 | `stock_code`（纯数字字符串，如 `000001`），跨市场用 `ts_code`（如 `000001.SZ`） | 与 Tushare 风格兼容 |
| 日期列 | 统一 `trade_date`（交易日）/ `calendar_date` / `event_date` | date 类型 |
| 时间戳 | 记录写入/更新时间统一 `update_time`（datetime） | 前端可直接展示 |
| 中文注释 | **每表必须有 table_comment、每列必须有 column_comment**（2026-09-05 已补 `stock_market_daily` = '股票行情历史数据表'；剩余 `stock_market_daily_bak_20250802` 与各列注释列入 G7 P2 治理） | 前端/数据中心直接读 |
| 唯一键 | 业务幂等必须 (自然键) 唯一索引，配合 INSERT IGNORE | (stock_code, trade_date) |
| 保留策略 | 高频流数据需明确保留窗口（见 §5.1 news 观察） | — |

---

## 3. 采集作业体系

### 3.1 运行架构

```
task_config（12 行，enabled/cron/params 可在前端热改）
   │  APScheduler（Asia/Shanghai，coalesce+max_instances=1+misfire 1h 补跑）
   ▼
run_task(name) ── TaskRecorder 记录 task_runs（running → success/failed + 写入条数）
   │
   ▼
collectors/*.py（实现类）→ pymysql 写表
```

- **运行中保护**：同任务 2 小时内已有 `running` 记录则拒绝新触发，防并发双写
- **失败策略**：全失败 raise → 任务 failed；部分失败（有成功写入）→ warning 但任务记 success
- **幂等**：INSERT IGNORE / TRUNCATE 重建 / 最小行数护栏（快照 <1000 行拒绝覆盖）

### 3.2 已实现任务（15 个，TASKS 注册表；2026-09-05 补齐 6 个遗留 + DQ + 档案域两任务）

| 任务 | cron | 源 | 写表 | 说明/容错 |
|---|---|---|---|---|
| `stock_daily_incr` | `0 19 * * 1-5` | 东财→腾讯→新浪→Tushare 四级降级 | stock_market_daily | 从每股本地最新日起增量补齐；退市/停牌股失败不阻断全量（有写入则记成功） |
| `market_current_sync` | `30 18 * * 1-5` | 无外部源 | stock_market_current | 纯本地聚合日线最新交易日；TRUNCATE+全量重建 ~5,400 行；<1000 行护栏 |
| `news_fetch` | `*/30 * * * *` | 东财快讯 API + 财联社(akshare) | news | 双源容错（cls 失败仅告警）；INSERT IGNORE 兜底 |
| `trade_calendar_sync` | `30 2 * * 0` | 新浪交易日历(akshare) | trade_calendar | 补齐当年+次年 |
| `concept_market_sync` | `0 20 * * 1-5` | 同花顺(akshare) | ths_concept_market / ths_concept_info | 398 概念日线增量；新概念自动注册 309xxx；失败单概念不阻断 |
| `ai_concept_analysis` | 手动（enabled=0） | 豆包方舟 AI | finance_concept_analysis | 分析日历事件→关联概念；**无 ARK_API_KEY 时降级占位**（当前即此态） |
| `stock_info_sync` | `0 2 * * 1` | 东财全A→本地快照降级 | stock_info / stock_info_ex | 周更全市场名单；主源失败自动切 stock_market_current；不删退市旧行；list_date 按需回填；ex 表人工标记保留 |
| `concept_sync` | `0 2 * * 1` | 同花顺成分页→新浪降级 | ths_stock_concepts | 逐概念重建成分（DELETE+INSERT 小事务）；同花顺反爬/失败自动切新浪同名概念；双失败保留旧数据 |
| `fund_info_sync` | `0 3 1 * *` | 东财基金列表(akshare) | fund_info | 月更全量重建 ~27,800 行；<20,000 行护栏拒绝覆盖 |
| `index_market_sync` | `30 18 * * 1-5` | 东财指数→腾讯降级 | dc_index_market | 13 个主流指数逐日增量；东财含全字段，腾讯降级仅 OHLCV+自算涨跌（amount/turnover NULL） |
| `bond_profit_sync` | `0 8 * * 1-5` | 中债+美债(akshare bond_zh_us_rate) | bond_profit_daily | 中美国债 2/5/10/30y + spread 增量补齐（本地 max 之后） |
| `finance_calendar_sync` | `0 8 * * *` | 东财数据中心 RPT_CPH_FECALENDAR | finance_calendar | 每日拉未来 60 天窗口；窗口内 DELETE+重插幂等；data_source=EM-CAL 与 JY 历史并存 |
| `data_quality_check`（v1.1） | `30 20 * * *` | 无外部源（纯库内 SQL） | dq_report（只读 dq_rules + 各业务表） | 每日体检：读 dq_rules 31 条规则逐条执行 → 结果快照写 dq_report；大表限定最新时间切片秒级完成；同轮只跑一次（运行中保护） |
| `stock_status_sync`（v1.3） | `0 8 * * 0` | Baostock query_stock_basic | stock_info（list_status/delist_date） | 周更全市场：覆盖 5,5xx 只上市/退市状态 + 退市日；ipoDate 顺带补 list_date NULL（已补 231 只）；B股/北交所不在源内保持 NULL 不误标 |
| `stock_company_sync`（v1.3/v1.4） | `30 8 * * *` | 巨潮官方 stock_profile_cninfo | stock_info（档案列，列级 UPDATE） | 日更差量：只处理档案缺失或 profile_updated_at >30 天未刷新的沪深在市 A 股（max_count 200/轮）；**v1.4 起写 stock_info 宽表档案列 + profile_updated_at=NOW()，不触碰证券列**（short_name/exchange/list_date/list_status/delist_date）；B股/北交所/退市跳过 |

> cron 调整说明：`bond_profit_sync` 由「每月 5 日」、`index_market_sync` 由「每月 10 日」调整为工作日日频——两者均为日频数据，原月更配置会让数据长期滞后（2026-09-05 调整）。

### 3.3 遗留未实现任务 → 已全部补齐（2026-09-05）

> 原 6 个任务"配置在但 TASKS 无代码 → 调度器静默跳过"（误导性 enabled=1）已全部实现并注册，
> 见 §3.2 追加行。补齐记录（含 2025 年最后数据基线，供对比）：

| 任务 | 原 cron → 现 cron | 对应表 | 原最后数据 → 补齐后 | 数据源选型 |
|---|---|---|---|---|
| `stock_info_sync` | `0 2 * * 1`（不变） | stock_info / stock_info_ex | 2025-09-27 → 2026-09-05 | 东财全A（生产）→ 本地快照（实测降级链路） |
| `concept_sync` | `0 2 * * 1`（不变） | ths_stock_concepts | 2025-09-20 → 2026-09-05（部分概念） | 同花顺成分页（生产）→ 新浪 gn_ 同名概念（实测 58/398 覆盖） |
| `fund_info_sync` | `0 3 1 * *`（不变） | fund_info | 2025-08-16 → 2026-09-05（27,790 行） | 东财基金列表（实测全量） |
| `index_market_sync` | `0 3 10 * *` → `30 18 * * 1-5` | dc_index_market | 2025-09-22 → 2026-09-04（12/13 指数） | 东财指数历史（生产）→ 腾讯日K（实测降级，数值与库内一致） |
| `bond_profit_sync` | `0 3 5 * *` → `0 8 * * 1-5` | bond_profit_daily | 2025-09-05 → 2026-09-04（9,336 行） | akshare bond_zh_us_rate（实测全量） |
| `finance_calendar_sync` | `0 8 * * *`（不变） | finance_calendar | 2026-01-06 → 2026-09-05（每日 60 天窗口） | **东财 RPT_CPH_FECALENDAR（换源重建，替代失效 JY）**（实测全量） |

> 本次代码全部落库 `backend/app/collectors/*_sync.py`；task_config cron 已按数据频率调整（bond/index 日频）。
> **沙箱限制说明**：东财 push2 行情族接口（stock_zh_a_spot_em / index_zh_a_hist / THS 成分页）在开发沙箱网络被限，
> 代码按生产直连实现并带降级链；其中 fund/bond/calendar/腾讯降级路径已在沙箱端到端验证，
> 需部署环境首跑确认的 3 项：东财全A 名单、东财指数主源、同花顺概念成分（住宅/服务器 IP 可用性更高）。

---

## 4. 数据源与可靠性

### 4.1 数据源清单

| 数据源 | 用途 | 接入方式 | 风控特征 | 可靠性 |
|---|---|---|---|---|
| 东方财富 | 日线主源、快讯、股票列表 | akshare `stock_zh_a_hist` + 快讯 HTTP API | 高频易触发风控/限流 | ★★★★ 主源 |
| 腾讯行情 | 日线降级 2 | akshare `stock_zh_a_hist_tx` | 偶发返回空/畸形数据 | ★★★ |
| 新浪财经 | 日线降级 3、交易日历 | akshare `stock_zh_a_daily` / `tool_trade_date_hist_sina` | 稳定 | ★★★★ |
| Tushare | 日线降级 4 | akshare `stock_zh_a_daily`(tu 通道) | 需 token、积分门槛 | ★★★ 保底 |
| 同花顺 | 概念清单/K线/分红 | akshare `stock_board_concept_*` | 名称匹配依赖上游 | ★★★★ |
| 财联社 | 电报快讯 | akshare `stock_info_global_cls` | 内部签名，偶发不可用 | ★★★ 容错源 |
| 豆包方舟 AI | 事件×概念分析 | OpenAI 兼容 HTTP | 需 ARK_API_KEY | ⚠️ 当前未激活 |

### 4.2 降级链（已实现的关键韧性设计）

日线采集四级降级，单只股票某源失败自动尝试下一源：

```
东财 stock_zh_a_hist ──失败──► 腾讯 ──失败──► 新浪 ──失败──► Tushare ──全败──► 记录错误（不阻断全量）
```

配套防御：`NO_PROXY=*`（国内源不走代理）、socket 级 15s 超时（防退市股卡死）、随机延时（降风控）。

---

## 5. 数据质量与健康保障

### 5.1 已落地的质量机制

| 机制 | 位置 | 作用 |
|---|---|---|
| 自然键唯一索引 + INSERT IGNORE | 各写表 SQL | 幂等去重，防并发/重跑双写 |
| TRUNCATE + 最小行数护栏 | market_current_sync | 快照全量重建，<1000 行拒绝覆盖防清空 |
| 部分失败容忍 | run.py | 有写入即记成功；全失败才 raise |
| 运行中保护 + 2h 过期 | scheduler.py | 防同任务并发双写，防脏 running 卡死 |
| 任务级审计 | task_runs | 每次执行留痕（状态/耗时/写入条数/错误） |
| 调度热配置 | task_config + sync_from_db | 前端改 cron/启停即时生效 |
| 数据中心只读闸门 | db_browser.py | 表名/列名白名单 + 参数化绑定，杜绝写面 |
| **DQ 自动体检**（v1.1） | dq_rules + dq_report + data_quality_check | 31 条规则每轮跑一次（10 类检查器：freshness_daily/freshness_interval/date_floor/row_count_slice/row_count_total/null_rate_slice/violation_count/where_count/regex_count/unique_index），红黄绿 + 趋势卡；每日 20:30 自动；规则表可热启停（§10.3） |
| **DQ 查询 API**（v1.1+） | `backend/app/api/quality.py` | 7 端点：GET `/api/dq/{summary,report,rules,history,table-status}` + POST `/api/dq/trigger` + PUT `/api/dq/rules/{name}`；全部只读（trigger 带运行中保护）；table-status 为 v1.2 新增按表聚合 |
| **数据中心质量叠加**（v1.2） | DataView.vue + `/api/dq/table-status` | 左侧每行质量状态点（绿通过/黄提醒/红异常/灰未纳入体检）+ 右侧选中表质量摘要块（体检时间/计数/失败规则明细）+ 「查看质量报告」跳转 `/quality`，实现「看表即知健康」 |
| **上市/退市状态**（v1.3） | stock_status_sync + stock_info | Baostock 官方全量状态（上市/退市）+ 退市日期列；空 list_date 回填率 307→76（余 B 股/北交所源外） |
| **公司档案宽表**（v1.3/v1.4） | stock_company_sync + stock_info 档案列 | 巨潮官方 24 字段（全称/主营/经营范围/机构简介/法人/注册地址等）差量日更；**v1.4 并入 stock_info 宽表**（证券级 + 档案级 1:1 合一，34 列）；`profile_updated_at` 独立追踪档案刷新，与证券周更解耦 |

### 5.2 盘点日已知问题（2026-09-05 实测，详见 §8）

1. **`stock_market_daily` 缺 09-02/03/04 三个交易日** —— 08-17~09-01 每交易日 ~5,127-5,131 行连续，之后断档；增量任务 09-04 19:00 仅写 37 行（异常）。已影响：行情快照与全部行情看板停留在 **09-01 收盘**。
2. **`news_fetch` 间歇 1062 重复键失败** —— 同一新闻跨天被反复拉取（如 em 9/3 一条从 21:00 到 23:30 连败 6 次）；`cls-` 空 url 记录撞唯一键。已有 INSERT IGNORE 但仍有漏网路径，需排查。
3. **`finance_calendar` 停更于 2026-01-06** —— 旧 JY 源失效，AI 概念分析（`finance_concept_analysis`）随之停摆 + 无 ARK_API_KEY 降级占位。
4. **18 张历史表无采集实现**（§2.2 🔴），数据价值仍在但不可保鲜。
5. **`stock_market_daily_ex` 注释缺失、与 daily 的关系口径未文档化**；备份表 `_bak_20250802` 占 1.9GB 待归档决策。
6. **news 保留策略未定义** —— 当前仅 648 条（约 2 天），需明确窗口（如 90 天）避免历史资讯不可查。

### 5.3 健康度判定建议（v1.1：已落地为自动化规则集）

> 原 §5.3 为判定建议；v1.1 已落地为 `dq_rules` 31 条可热配规则（详见 §10.3）+ 前端「数据质量」栏目红黄绿展示 + 趋势卡。

| 表类型 | 判定规则 | 落地为规则 | 示例 |
|---|---|---|---|
| 日频（对齐交易日历） | 最新 trade_date = 上一交易日则通过；落后 ≥1 标提醒，≥3 标异常 | freshness_daily | stock_market_daily.daily_freshness |
| 高频流 | 距当前 < pass_hours 通过，< fail_hours 提醒，否则异常 | freshness_interval | news.news_freshness |
| 日期下限类 | MAX(date) ≥ today - days_back | date_floor | finance_calendar.finance_calendar_fresh |
| 体量 | 切片 ≥min_rows / 总 ≥min | row_count_slice / row_count_total | concept_market_rows / fund_info_rows |
| 完整性 | 最新切片某列空值率 ≤max_pct | null_rate_slice | daily_close_null |
| 业务校验 | 切片脏数据 ≤max_count | violation_count | daily_value_bounds |
| 代码规范 | 全表正则不匹配数 =0 | regex_count | stock_info_code |
| 结构 | 期望唯一索引存在 | unique_index | news_uniq |

---

## 6. 分析资产：从数据到价值

### 6.1 分析模板与 API（backend/app/analysis + api/analysis.py）

| 模块 | 能力 | 数据依赖 | API | 前端消费 |
|---|---|---|---|---|
| `ytd.py` | 年初至今涨幅排行 | stock_market_current.ytd_change_pct | `/api/analysis/ytd-rank` | 分析栏目 |
| `dividend.py` | 股息率排行（按报告期） | ths_stock_dividend + 最新行情 | `/api/analysis/dividend-rank` | 分析栏目 |
| `concept_rank.py` | 概念板块轮动（单日+N日区间涨幅） | ths_concept_market | `/api/analysis/concept-rank` | 概念栏目 |
| `concept_ai.py` | 日历事件→概念 AI 归因 | finance_calendar + 豆包 | `/api/ai/...` | 日历栏目 |

### 6.2 早期手工分析 SQL 备忘

> 项目早期沉淀过 5 套分析 SQL（概念排名 / 市值 / 年初至今涨幅 / 分红率 / 机构调研热度），
> 其中概念排名、YTD、分红已固化为上方 Python 模块；**市值、机构调研热度**两套尚未模块化，
> 机构调研热度对应 `stock_jgdy_detail`（死表，需先恢复采集）。历史 SQL 散落在开发期工作区，未入库 —— 建议后续挑选价值高的固化进 analysis/。

### 6.3 数据 ↔ 产品页面映射

| 前端栏目 | 页面 | 依赖表 | 数据健康依赖 |
|---|---|---|---|
| 行情看板 | MarketView + KLineChart | stock_market_current / stock_market_daily | 🟡 受日线缺口影响 |
| 概念 | ConceptView | ths_concept_market/info + finance_concept_analysis | 🟢 / 🟡(AI) |
| 日历 | CalendarView | finance_calendar + AI 分析 | 🟡 停更 |
| 资讯 | NewsView | news | 🟢（含间歇失败） |
| 分析 | AnalysisView | /analysis/* 三排行 | 🟡 分红表死 |
| 数据中心 | DataView | 全 27 表（只读） | — |
| 作业监控 | JobsView | task_config / task_runs | 🟢 |

---

## 7. 治理原则（新增资产必须遵守）

1. **登记优先**：新增表/字段必须同步更新本文档 §2.2 清单（表注释写进 DDL）
2. **实现优先于配置**：`task_config` 只允许登记**已在 TASKS 注册**的任务；未实现任务一律 enabled=0，杜绝"看起来在跑"
3. **源必留痕**：每个采集器 docstring 必须写数据源接口与降级策略；每个数据域标注来源
4. **幂等是底线**：所有写表必须可重跑（INSERT IGNORE / 唯一键 / TRUNCATE 重建）
5. **读取不写**：网页层只读；任何写/改/删只允许发生在 MySQL 客户端或采集器内
6. **密钥不落库**：API Key 一律环境变量（当前 `ARK_API_KEY` 已规范，待重新申请激活）
7. **指标可复算**：分析 SQL 的每个数字要能追溯到源表（避免"死数字"）

---

## 8. 已知数据缺口与风险清单

| # | 级别 | 缺口 | 影响 | 建议处置 |
|---|---|---|---|---|
| G1 | **P0** | 日线缺 09-02~04 | 行情看板/快照/分析全部停留在 09-01 | 手动触发一次增量观察失败根因（疑似接口或 qfq 参数）；必要时单源修复 |
| G2 | **P0** | ARK_API_KEY 失效 | AI 日历分析 2 个月占位 | 重新申请方舟 Key → 写入环境变量 → 重启 → 手动触发 ai_concept_analysis |
| G3 | **P0** | news_fetch 间歇 1062 | 每 30 分钟任务偶发失败 | 排查漏网 INSERT 路径（疑似非 IGNORE 分支）；对 url 为空的 cls 记录降级 |
| G4 | P1 | finance_calendar 停更 8 个月 | 日历栏目 + AI 无新事件 | 换源（如东财日历接口/akshare 替代）重建 finance_calendar_sync |
| G5 | P1 | 16 张历史死表 | 分红/调研/资金流等分析缺新数据 | 按价值排序逐个恢复采集器（§9 P1），先恢复 ths_stock_dividend（分红排行在用） |
| G6 | P2 | 日线衍生/备份表未归档 | 7GB 存储，口径混乱 | 归档 bak 表；明确 daily_ex 口径或删除 |
| G7 | P2 | 表/列注释缺失 | 数据中心与维护体验 | 批量补 `stock_market_daily` 等表注释（2026-09-05 部分：补 `stock_market_daily`；剩余 `stock_market_daily_bak_20250802` 与所有列注释待办） |
| G8 | P2 | news 无保留策略 | 历史资讯不可查 | 定义保留窗口与清理任务 |

---

## 9. 优化规划路线图

### P0 · 止血（本周级）
- [x] 补齐 6 个未实现同步作业（§3.3，2026-09-05 已完成；原「统一置 enabled=0 消除误导」方案作废）
- [ ] 排查并补齐日线 09-02~04（G1）—— 验收标准：`stock_market_daily` MAX(trade_date) = 最近交易日且当日行数 ≥5,000
- [ ] 重新申请 ARK_API_KEY 并激活 AI（G2）
- [ ] 修复 news_fetch 重复键漏网路径（G3）
- [x] 实施 DQ 自动体检 + 数据质量栏目（v1.1，2026-09-05 已完成；原 §9 P1 「数据资产大盘」以「自动体检」形态落地）
- [x] 数据中心叠加质量结果（v1.2，2026-09-05 已完成：表清单状态点 + 选中表质量摘要 + 跳质量栏目；原 P1「全表徽章总览」部分形态落地）

### P1 · 活水（月级）
- [x] 实现 `数据资产大盘` 健康看板 → **已分两阶段落地**：v1.1 DQ 自动体检（数据质量栏目：红黄绿 + 通过率进度 + 14 天趋势）+ v1.2 数据中心叠加质量标记（看表即知健康）；后续可选：所有 29 表「未纳入体检」规则补全、健康徽章总览首页
- [x] 重建 `finance_calendar_sync` 换源（G4，2026-09-05 已完成：东财 RPT_CPH_FECALENDAR）
- [ ] 按价值恢复历史表采集：`ths_stock_dividend`（分红排行在用）→ `securities_margin` → `stock_capital_flow` → `stock_jgdy_detail`
- [ ] JobsView 运行详情 → "查看数据"跳 `/#/data?table=xxx` 打通"作业成功→数据实况"

### P2 · 增值（季度级）
- [ ] 存储治理：归档 `_bak_20250802`、明确/删除 `stock_market_daily_ex`（G6，立省 ~5GB）
- [ ] 补全表/列注释（G7）；定义 news 保留窗口（G8）
- [ ] 采集可观测：任务级耗时/写入量趋势 mini 图、数据源降级次数统计
- [ ] 早期手工 SQL 挑选固化（市值/机构调研热度，§6.2）
- [ ] 跨表交叉分析储备：日线×分红（股息率年化）、日线×概念（轮动强度）、两融×日线（杠杆情绪）

### 决策待定（建议尽快拍板，影响 P1 排序）
1. **历史死表去留**：16 张全保留恢复 vs 只保价值前 5 张、其余冻结归档
2. **行情口径**：`stock_market_daily`（qfq 前复权）已够用，`_ex` 是否需要
3. **栏目命名**：数据资产大盘是否立项、叫什么（延续「数据中心」只读风格 or 健康看板）

---

## 10. 附录

### 10.1 本次盘点方法（可复用）
1. `information_schema.tables` 全量清单（估算行数/体积/表注释/update_time）—— 注意 **MySQL 8 返回大写列名**
2. 逐表探测时间列 `MAX()`（仅当列有索引，避免大表全扫）
3. `task_config` + `task_runs` 快照 → 任务-表映射与失败历史
4. TASKS 注册表 vs task_config 求差 → 未实现任务清单
5. 源码 grep 表名引用 → 判定"死表"（无任何读写引用）
6. Git 历史核对演进（Initial commit 仅 README，采集体系随 MVP 4fbb4d2 一并入库）

### 10.2 关联资产
- 设计规范：`docs/design/颜色体系设计规范.md`（本规范的同级资产）
- 交付说明：`artifacts/`（各次功能交付均有对应说明）

### 10.3 DQ 规则集清单（v1.1 起，31 条，2026-09-05 实盘）

**载体与运维**
- 规则存 `dq_rules` 表；种子脚本 `backend/scripts/seed_dq_rules.py`（幂等，可重复执行，阈值注释含校准依据）
- 建表脚本 `backend/scripts/init_dq_tables.py`；启停热生效（前端规则配置 / `PUT /api/dq/rules/{rule_name}`）；大表一律限定「最新时间切片」秒级完成
- 10 类检查器：`freshness_daily`（对齐交易日历）/ `freshness_interval`（高频流间隔）/ `date_floor`（事件窗口下限）/ `row_count_slice`（最新切片行数）/ `row_count_total`（全表行数护栏）/ `null_rate_slice`（空值率）/ `violation_count`（最新切片自定义 where 脏值数）/ `where_count`（全表任意条件行数，静态档案表用，v1.4 新增）/ `regex_count`（代码格式）/ `unique_index`（自然键唯一索引存在性）

**覆盖矩阵（31 条 = 12 张自更新表）**

| 表 | 规则（severity） |
|---|---|
| stock_market_daily | daily_freshness(critical) / daily_rows_latest(critical) / daily_close_null / daily_value_bounds / daily_natural_key |
| dc_index_market | index_freshness / index_rows_latest |
| stock_market_current | current_rows(critical) / current_code |
| ths_concept_market | concept_market_freshness / concept_market_rows(critical) / ths_concept_market_uniq(info) |
| ths_concept_info | concept_info_rows |
| ths_stock_concepts | stock_concepts_rows / stock_concepts_code |
| news | news_freshness / news_rows / news_uniq(info) |
| finance_calendar | finance_calendar_fresh(critical) / finance_calendar_rows |
| bond_profit_daily | bond_freshness |
| fund_info | fund_info_rows |
| stock_info / _ex | stock_info_rows / stock_info_code / **stock_info_delist_mark / stock_info_delist_date / stock_info_profile_cover**（v1.4 退市一致性 + 档案覆盖）/ stock_info_ex_rows(info) / stock_info_ex_code(info) |
| trade_calendar | trade_calendar_rows(info) / trade_calendar_uniq(info) |

**首轮战果**（2026-09-05 20:30 实盘；无需人工逐表即抓到 3 个真实缺口）
1. `daily_freshness` fail/critical —— 日线停 09-01 落后 3 交易日（= §5.2 G1 / §8 G1）
2. `daily_natural_key` fail/warning —— 缺 (stock_code,trade_date) 唯一索引（新增发现）
3. `concept_market_rows` fail/critical —— 概念行情 9/4 仅 22 行（正常 ~375，**新发现缺口**）
- v1.2 复测：13 张体检表 11 过 2 红（上两项未修复前恒红）；14 张静态/历史表未纳入（数据中心灰点），补规则后自动纳入
- 数据中心栏目实现：`backend/app/api/db_browser.py` + `frontend/src/views/DataView.vue`（本规范 §2.2 的"活目录"，可随时肉眼核对）

*本文档为 InvestBuddy 数据资产基线 v1.0，盘点于 2026-09-05。数据状态随优化迭代变化，P0/P1 完成后应刷新 §5.2 / §8 / §9 勾选状态，升级 v1.1。*
