# 货币流动性 观测体系设计 v0.2（已拍板 · 已落地）

> 版本：2026-09-25 · 状态：**v0.2 已落地**（D1~D6 全部拍板并实施；含 §2.1 / §4.4 两处勘误）
> 上游文档：《分析研究模块交互设计规范》v1.3 §2.1 / §15
> 实证底稿：`_scratch/_probe_liquidity.py`、`_probe_ak_liq.py`、`_probe_ak_liq2.py`、`_probe_liq_calc.py`
> （本文所有「状态」「覆盖」「偏差」均为**实跑实测**，非文档推断）
>
> 🔴 **v0.1 → v0.2 勘误（两处实测推翻旧结论，正文已就地标注）**：
> 1. DXY 偏差「−3.09% / ±3% 系统性偏离」→ **标价法漏除 100 的假偏差**，修正后 **+0.085%**（§2.1）；
> 2. 「人民币中间价 26 币种」→ 实为 **17 币种 / 40,603 行**；`currency_boc_safe` 是 akshare 接口名、非本库表名（§6 / §2.3）。

---

## 0. 本次已落地的变更（更名）

| 项 | 改前 | 改后 |
|---|---|---|
| 领域名（`registry.group` / detail `name`） | 钱贵不贵 | **货币流动性** |
| 卡片墙组标题 | 钱贵不贵 | **货币流动性** |
| 详情页头部标签 | 钱贵不贵 | **货币流动性** |
| 模块 docstring / API 注释 / 视图注释 | 钱贵不贵 | 货币流动性 |

**改的**：`registry.py`（4 处 `group` + detail `name` + 分节注释 + 1 处 drilldown label）、
`money_cost.py`（docstring ×3 + 注释）、`api/analysis.py`、`MoneyCostView.vue`。

**刻意不改的**：`collectors/`、`scripts/seed_*.py` 里的「蓝图A 钱贵不贵」——
那是**历史代号**，改了会丢失与旧报告/旧 DQ 规则的对应关系（同「蓝图D/E」的处理）。
已在 `registry.py` 该分节顶部注明更名时间、原名、以及为什么保留历史引用。

**为什么不叫「钱贵不贵」**：原名只覆盖**价格**一维（利率高不高），
而货币流动性要回答三件事——**钱贵不贵（价格）· 钱多不多（数量）· 央行在做什么（行为）**。
名字窄了，指标体系就会被名字带窄。

---

## 1. 观测框架：四维 × 五区

货币流动性无法用单一指标刻画。本方案用**两个正交切面**定位每一个待观测指标：

### 1.1 四个维度（看什么）

| 维度 | 回答 | 典型指标 | 为什么必须有 |
|---|---|---|---|
| **① 价格** | 钱的价格（利率） | 政策利率、Shibor/DR007、国债收益率曲线 | 利率是估值分母；且**分位**比绝对值重要 |
| **② 数量** | 钱的总量与增速 | M0/M1/M2、社融、信贷、央行资产规模 | 价格只反映「边际供需」，数量反映「水位」 |
| **③ 央行行为** | 谁在放、谁在收、节奏 | 利率决议、准备金率、资产负债表扩张/收缩 | 流动性是**政策变量**，不是自然现象 |
| **④ 外部约束** | 跨境资金往哪走 | 汇率、美元指数、中外利差、外汇储备 | 开放经济下，国内流动性受外部硬约束 |

### 1.2 五个区域（看哪里）

**全球 → 美国 → 中国 → 日韩欧**。这不是并列关系，而是**传导链**：

```
   美国（全球美元流动性的源头：美联储 + 美债）
        │  美元指数 / 美债利率 = 全球风险资产的贴现率
        ▼
   全球（美元指数 + 主要央行方向共振）
        │
        ├──► 中国（本土政策主导，但受中美利差/汇率约束）
        │
        └──► 日韩欧（开放小型/中型经济体，对美元周期最敏感）
```

**为什么美国单独一层**：美元是全球流动性的**清算货币**。美联储的松紧直接决定全球美元供给，
其他央行在很大程度上是**跟随者或对冲者**（ECB/BOJ 的决策要减去美联储的影响才看得清）。
把美国和中国并列是新手常见的错——它们的**因果层级不同**。

### 1.3 「价 × 量」四象限（怎么下结论）

单看一个维度会误判。**价格与数量的组合**才是流动性的真实状态：

| | 数量扩张（量升） | 数量收缩（量降） |
|---|---|---|
| **价格下行（价降）** | 🟢 **主动宽松**：央行放水且信用在扩张，对权益最友好 | 🟡 **被动宽松 / 需求不足**：钱便宜但没人借 → 流动性陷阱风险，**警惕「宽松无效」** |
| **价格上行（价升）** | 🟠 **需求驱动**：资金需求旺盛，经济动能强 → 但对估值是逆风 | 🔴 **被动收紧**：央行收水或信用收缩 → 权益最危险组合 |

> ⚠️ 这张表是「货币流动性」模块的**判断骨架**——所有指标最终都是为了把当前状态放到这四个格子里。
> 只报数字不给格子，就又退化成「念 KPI」。

---

## 2. 指标清单（按区域）

图例：🟢 库里已有 · 🟡 可采（接口实测可用）· 🔴 实测拿不到 · ⭐ 建议优先落地

### 2.1 全球层

| # | 指标 | 维度 | 数据源（实测） | 状态 | 能回答什么 |
|---|---|---|---|---|---|
| G1 | **美元指数 DXY** | 外部约束 | 自算（6 成分货币交叉汇率）+ 新浪 `DINIW` 快照对账 | 🟡⭐ | 全球美元松紧的**总闸门**；美元走强 = 全球流动性收紧 |
| G2 | **主要央行政策利率（美/欧/日/英）** | 央行行为 | `macro_bank_{usa,euro,japan,english}_interest_rate` | 🟡⭐ | 全球央行**方向共振度**（同向宽松 = 全球risk-on 环境） |
| G3 | 主要经济体 10Y 国债收益率 | 价格 | 美债已有；欧/日需另找 | 🟡 | 全球无风险利率中枢 |
| G4 | 全球央行资产负债表合计 | 数量 | — | 🔴 | 全球流动性总水位（**无免费源，暂缺**） |

**G1 实测细节（重要）**：
- 官方历史源全部实测失败：东财 `push2his` 域名不通（kline 路径被拦）、
  新浪 `gi.finance.sina.com.cn/hq/daily` 不支持 `UDI/DINIW`、`futures_foreign_hist('DX')` 只返 13 行（2019 年）
- **自算方案可行**：`currency_boc_daily` 含 ICE DXY 全部 6 个成分货币（欧元/日元/英镑/加元/瑞典克朗/瑞士法郎），
  按标准公式加权（落地采集器 `usd_index_sync`），实测回填 **3,650 天（`first_lookback_days=3650`，2016 起）**
- ~~**偏差实测 −3.09%**（自算 98.11 vs 真实快照 101.23）——根因是**人民币中间价 ≠ 市场汇率**（中间价是参考一篮子定的官方价）~~
- 🔴 **❌ 勘误（2026-09-25 落地时被实测推翻）**：上面的「−3.09%、±3% 系统性偏离」是**标价法单位算错的假偏差**。
  根因：`currency_boc_daily` 里美元是**直接标价「人民币/100 外币」**（674.89 → 必须 ÷100 得 6.7489），
  当时漏除 100 → 自算值整体偏小 → 被误读成「人民币中间价口径与 ICE 官方有系统性偏离」。
  **修正后实测：自算 101.35 vs 新浪 `DINIW` 快照 101.2675 → 偏差 +0.085%**（2026-09-24 单点标定）。
  ⇒ 「±3% 系统性偏离」的结论**不成立**，两种口径其实高度一致。
- ⇒ 口径标注**修正为**：**「人民币中间价交叉汇率口径（自算），2026-09-24 与 ICE 官方 DXY 单点偏差 +0.085%；用于看趋势与相对位置」**
- 补充：新浪 `hq.sinajs.cn/list=DINIW` 实测返回真实美元指数实时值（101.2321）→ 已每日累积为**标定基准**
  （表列 `dxy_snapshot`，自 2026-09-24 起），可长期复核偏差。
- 🔒 **固化守护**：DQ 规则 `dxy_vs_snapshot`（`ABS(dxy_calc − dxy_snapshot) > 1.5` → 期望 0 行），
  这是「标价法单位错的通用网」—— 任何币种单位换算回归错误都会立刻触发。

### 2.2 美国层（全球美元流动性的源头）

| # | 指标 | 维度 | 数据源（实测） | 状态 | 能回答什么 |
|---|---|---|---|---|---|
| U1 | **联邦基金目标利率 + 最近决议** | 央行行为 | `macro_bank_usa_interest_rate`（294 行，1982 起） | 🟡⭐ | 全球流动性的**总开关**；连续未动月数 = 政策耐心 |
| U2 | **美债 2Y/5Y/10Y/30Y** | 价格 | `bond_profit_daily`（8,946 行，1990 起，95.7% 非空） | 🟢⭐ | 全球资产定价锚 |
| U3 | **美债 10Y−2Y 利差** | 价格 | 同表已算好（`us_bond_10y_2y_spread`） | 🟢⭐ | **倒挂 = 衰退预警**（1980 以来最可靠的单一信号） |
| U4 | **中美 10Y 利差** | 外部约束 | 库里两列相减（`cn_bond_10y − us_bond_10y`） | 🟢⭐ | 中国资产相对美国的**利差吸引力**；倒挂加深 = 资本外流压力 |
| U5 | SOFR / EFFR | 价格 | — | 🔴 | 美国货币市场**实际**成交利率（比政策利率更真实） |
| U6 | 美联储资产负债表（H.4.1） | 数量 | — | 🔴 | QE/QT 规模（**akshare 无接口**） |
| U7 | ON RRP / 准备金余额 | 数量 | — | 🔴 | 美元流动性最敏感的边际变量 |

> **美国层的结构性缺口**：akshare 只提供**价格维度**（政策利率 + 国债曲线），
> **数量维度（美联储资产负债表、准备金、ON RRP）完全没有**。
> 这是本方案最大的数据缺口。短期用 U1~U4 四件套（价格 + 期限结构 + 中外利差）已能刻画「美国松紧的方向与斜率」，
> 但无法回答「美联储的资产负债表在扩还是在缩」。若要补，需接入 FRED API（另需 API Key，属新增外部依赖，见 §6 决策项）。

### 2.3 中国层（对 A 股最直接）

#### 价格维度

| # | 指标 | 数据源（实测） | 状态 |
|---|---|---|---|
| C1 | **Shibor ON/1W/1M/3M** | `interbank_rate_daily`（4,989 行，2006-10 起，100% 非空） | 🟢 |
| C2 | Shibor 2W/6M/9M/1Y（补全 8 档） | `macro_china_shibor_all`（2,375 行，2015-05 起） | 🟡 |
| C3 | **DR007 / FR001 / FR007 / FR014**（银行间质押式回购） | `repo_rate_query`（748 行，2023-09 起） | 🟡⭐ |
| C4 | **LPR 1Y / 5Y** + 连续未动月数 | `interbank_rate_daily`（3,225 / 1,771 行） | 🟢 |
| C5 | **中债 2Y/5Y/10Y/30Y + 10Y−2Y** | `bond_profit_daily`（6,179 行） | 🟢 |

> **C3 的战略价值**：DR007 是**央行公开市场操作的目标利率**——它比 Shibor 更贴近政策意图。
> Shibor 是报价利率（银行报出来的），DR007 是真金白银的成交利率。
> 央行「引导 DR007 围绕政策利率波动」是官方表述，所以 **DR007 与政策利率的偏离 = 央行态度是否真的宽松**。
> 实测只有 2023-09 起的数据（约 750 行），够做近一年分位与趋势。

#### 数量维度

| # | 指标 | 数据源（实测） | 状态 | 能回答什么 |
|---|---|---|---|---|
| C6 | **M0 / M1 / M2**（量 + 同比 + 环比） | `macro_china_money_supply`（224 行 → 2026-08） | 🟡⭐ | 货币总量水位 |
| C7 | **M1−M2 剪刀差**（派生） | 由 C6 相减 | 🟡⭐ | **资金活化度**——剪刀差收窄 = 钱从定期转向活期 = 实体意愿回升。比 M2 单看更有信息量 |
| C8 | **社融增量 + 分项**（人民币贷款/企业债/股票融资…） | `macro_china_shrzgm`（136 行，2015-01 起） | 🟡⭐ | **信用扩张的真实规模**（M2 是负债端，社融是资产端） |
| C9 | 新增人民币信贷 | `macro_china_new_financial_credit`（224 行） | 🟡 | 银行放贷意愿 |
| C10 | **存款准备金率** | `macro_china_reserve_requirement_ratio`（58 行） | 🟡 | 央行的**数量型工具**（降准 = 直接放水，与价格型工具 LPR 互补） |
| C11 | **央行资产负债表**（总资产 / 储备货币 / 对其他存款性公司债权 / 外汇 / 政府存款） | `macro_china_central_bank_balance`（356 行，28 列 → 2026.8） | 🟡⭐ | **中国式 QE 的直接观测** |
| C12 | 外汇储备 | `macro_china_fx_reserves_yearly`（132 行 → 2025-09） | 🟡 | 央行干预汇率的能力与消耗 |

> **C11 是本方案最有价值的单项新增**。「对其他存款性公司债权」这一科目就是央行通过
> MLF/逆回购/PSL 投放给银行的资金——**它扩张 = 央行在放水，收缩 = 在收水**。
> 这在中国没有官方「QE 规模」公告，只能从资产负债表倒推。实测该科目 2026-08 = 21.34 万亿。
> 三条派生指标：① 总资产同比 ② 「对其他存款性公司债权」环比变化 ③ 「外汇占款」vs「主动投放」的结构切换
> （2014 年前靠外汇占款被动投放，之后转为主动投放——这是中国货币投放机制的**分水岭**）。

#### 外部约束维度

| # | 指标 | 数据源（实测） | 状态 |
|---|---|---|---|
| C13 | 人民币中间价（17 币种，含 USD/EUR/JPY/HKD/GBP/KRW 等） | `currency_boc_daily`（**40,603 行 / 17 币种**，实测） | 🟢 |
| C14 | ~~人民币中间价 26 币种全量~~ → **实为 17 币种** | ~~`currency_boc_safe`（8,056 行，1994 起）~~ → 🔴 **勘误：本库无此表**；`currency_boc_safe` 是 **akshare 的接口名**（返回 26 币种宽表），被误写成了本库表名。落地表一直是 `currency_boc_daily` | 🟡 |
| C15 | 离岸人民币 CNH / 在岸 CNY | 新浪 `fx_susdcnh` / `fx_susdcny`（实测可用，实时） | 🟡 |
| C16 | 融资融券余额 | `securities_margin`（3,941 行） | 🟢 |

> 🔴 **勘误（2026-09-25 落地时回实库核对）**：本表初稿把「26 币种」记成了 `currency_boc_daily` 的属性。
> 实库实测：`currency_boc_daily` = **17 币种 / 40,603 行**（`SELECT COUNT(DISTINCT currency)`，非估算）。
> 26 是 akshare 接口 `currency_boc_safe` 的**宽表币种数**，本库从未落地该表（`SHOW TABLES LIKE 'currency%'`
> 只返回 `currency_boc_daily` 一张）。⇒ **凡涉及表名/行数/币种数，一律以实库 `COUNT` 为准。**
> 另：`currency_boc_daily` 混用**两种标价法**（详见 §4.4 勘误），是本轮最大的单位陷阱。

### 2.4 日韩欧层（对美元周期最敏感的开放经济体）

| # | 区域 | 指标 | 数据源（实测） | 状态 |
|---|---|---|---|---|
| A1 | 欧元区 | **ECB 政策利率** | `macro_bank_euro_interest_rate`（279 行，1999 起） | 🟡⭐ |
| A2 | 英国 | **英国央行 Bank Rate** | `macro_bank_english_interest_rate`（638 行，1970 起） | 🟡⭐ |
| A3 | 日本 | **日本央行政策利率** | `macro_bank_japan_interest_rate`（203 行，2008 起） | 🟡⭐ |
| A4 | 韩国 | 韩国央行基准利率 | — | 🔴 |
| A5 | 欧洲 | 德国 DAX / 法国 CAC40 / 英国 FTSE100 / 欧洲 Stoxx50 | 新浪 `gi.finance.sina.com.cn/hq/daily`（各 1,000 行，2022-09 起） | 🟡⭐ |
| A6 | 日本 | 日经 225 | 同上（1,000 行） | 🟡⭐ |
| A7 | 韩国 | 韩国 KOSPI | 同上（1,000 行） | 🟡⭐ |
| A8 | 三地 | 欧元 / 日元 / 韩元 / 英镑 汇率 | `currency_boc_daily`（17 币种，含全部 4 个） | 🟡 |

> **A4 韩国央行利率**：akshare **无** `macro_bank_korea_interest_rate`（实测 AttributeError），
> 且不提供替代接口。**韩国层只能靠 KOSPI + 韩元汇率**做「结果观测」，无法做「政策观测」。
> 这是本层唯一的缺口，暂接受（韩国对全球流动性的影响量级远小于美欧日）。

> **A5~A7 实测细节**：走**新浪环球指数**接口，不走 akshare 函数
> （`ak.index_global_hist_sina` 的符号映射表 key 对不上，实测 KeyError；
> `ak.index_global_hist_em` 因东财 kline 域名被拦全数失败）。
> ⚠️ 该接口 `num=10000` 实测**只返 1,000 行**（约 4 年历史），够算近一年分位与 20/60 日变化，
> 但**不够做长周期历史类比**。若需更长历史须另找源。

---

## 3. 「现状 / 变化 / 趋势 / 历史」四问的产品落地

用户明确要求：**现状、变化、趋势、历史**。这四问在每个维度上都要有落点，否则只是一个数字看板。

| 问 | 怎么算 | 前端落点 | 现状覆盖 |
|---|---|---|---|
| **现状** | 最新值 + 近一年分位 | KPI 数字 + 刻度条（`scale`） | ✅ 已有（`_kpi.scale`） |
| **变化** | 20 日 / 60 日绝对变化 | KPI `sub` 行（如「较 20 日前 +12bp」） | 🟡 部分有（`scale` 只给分位） |
| **趋势** | 连续同向期数（如「连升 5 期」）+ 拐点识别 | 判读条 `headline`（如「资金利率连续第 3 周回落」） | ❌ 缺 |
| **历史** | 极值 + 历史分位 + **历史类比** | 详情页专卡（如「当前形态类似 2013-06 / 2015-05」） | ❌ 缺（只有近一年分位） |

> **「历史类比」是最难也最有价值的一层**：算出当前（价格分位, 数量增速, 央行方向）三元组，
> 在历史上找最相似的时点，展示「当时之后 3/6 个月的 A 股表现」。
> ⚠️ 但这是**高风险的过度拟合**——样本少（中国货币周期约 3~4 轮）、伪相关多。
> **本方案不把它放进 v1**，列为 v2 候选（见 §6 决策项 D3）。

---

## 4. 实证算例（用现有数据**已经**能得出的结论）

> 这一节是方案的可行性证明：不是「理论上可以」，是**现在就能算出来**。

### 4.1 中美 10Y 利差（库里两列相减，零新增采集）

```
最近一期 2026-09-23:  中债10Y = 1.68%   美债10Y = 5.11%
中美利差 = −3.43pp
近 260 个交易日分位 = 0.4%   ← 近一年最窄（历史极值）
窗口: max −2.14 | min −3.43 | 中位 −2.48
```

**判读**：中美利差处于近一年**最窄**位置 ⇒ 人民币资产对海外资金的利差吸引力被压缩到极限，
构成资本外流与汇率贬值的**持续压力源**。这是「外部约束」维度最硬的一个数字，且**一行 SQL 就能算**。

### 4.2 M1−M2 剪刀差（可采，月度）

```
2026-08   M1 +4.1%   M2 +7.5%   剪刀差 −3.4pp
2026-07   M1 +4.0%   M2 +7.7%   剪刀差 −3.7pp
2026-06   M1 +4.0%   M2 +8.0%   剪刀差 −4.0pp
2025-09   M1 +7.2%   M2 +8.4%   剪刀差 −1.2pp   ← 一年前接近转正
```

**判读**：剪刀差从 2025-09 的 −1.2pp 走阔到 2026-06 的 −4.0pp、近两月略收窄（−3.4pp）。
资金活化度**过去一年在下降**（钱在往定期走，不是往活期走）⇒ 实体投资意愿偏弱。
近两月的边际收窄是**弱改善信号**，但仍在深度负值区。

### 4.3 央行资产负债表（可采，月度）

```
2026.8   总资产 498,567 亿   储备货币 401,814 亿   对其他存款性公司债权 213,399 亿   外汇 217,248 亿
2026.5   总资产 483,807 亿   储备货币 391,224 亿   对其他存款性公司债权 203,265 亿
```

**判读**：总资产 3 个月回升约 1.5 万亿，「对其他存款性公司债权」同步回升 1 万亿 ⇒
**央行在通过主动投放（MLF/逆回购）扩张资产负债表**，而非依赖外汇占款。
这与「M1−M2 剪刀差收窄」互相印证——**价×量四象限里落到「主动宽松」象限**。

### 4.4 自算美元指数（已落地）

```
最新自算 DXY = 101.35（global_usd_index_daily，回填 3,650 天）  近一年分位 96.0%   近 20 日 +1.83
真实快照（新浪 DINIW）= 101.2675   偏差 +0.085%（同一口径，见 §2.1 勘误）
```

🔴 **勘误**：本节初稿的「98.11 / 分位 91.2% / 偏差 −3.09%」全部**作废** —— 那是标价法漏除 100
的中间产物（同上）。按 2026-09-25 落地实测值替换。

**判读**：美元处于近一年**高位（96.0% 分位）**且在走强 ⇒ 全球美元流动性偏紧，
与 4.1 的中美利差极窄**同向印证**（美元强 + 利差窄 = 新兴市场资金流出压力）。

> 4.1 + 4.4 的**交叉印证**就是「全球层」与「中国层」的联接点——
> 这也是当前卡片墙「互证」卡想做的事，只是目前只有 A 股内部维度。

---

## 5. 数据能力与缺口总表

### 5.1 汇总

| 区域 | 价格 | 数量 | 央行行为 | 外部约束 |
|---|---|---|---|---|
| **全球** | 🟡 部分 | 🔴 | 🟡 4 国可采 | 🟡 美元指数自算 |
| **美国** | 🟢 曲线完整 | 🔴 **全缺** | 🟡 政策利率 | — |
| **中国** | 🟢 已有 + 🟡 可补 | 🟡 **全可采** | 🟡 LPR/准备金/央行表 | 🟢 汇率已有 |
| **日韩欧** | — | — | 🟡 欧/日/英，🔴 韩 | 🟡 汇率+股指 |

**结论**：**中国层可以做深（四个维度全部具备）**，美国层只能做「价格 + 政策」，
全球层靠自算美元指数 + 多国央行方向拼出来，日韩欧层做「结果观测」（股指 + 汇率）。

### 5.2 明确缺口（实测确认为「拿不到」）

| 缺口 | 影响 | 替代方案 |
|---|---|---|
| 美联储资产负债表 / 准备金 / ON RRP | 无法回答「美国在 QE 还是 QT」 | 用美债曲线 + 政策利率代理（**降级方案**）；或接 FRED API（需决策） |
| SOFR / EFFR | 无法看美国货币市场**实际**利率 | 用 2Y 美债近似（相关性高，但不是同一回事） |
| 韩国央行利率 | 韩国层无法做政策观测 | 用 KOSPI + 韩元汇率做结果观测 |
| 美元指数官方历史 | 官方历史源不可采（东财/新浪/外盘期货均实测失败），**并非口径偏差问题** | 自算 + `DINIW` 实时标定（实测偏差仅 **+0.085%**） |
| 日韩欧指数仅 4 年历史 | 无法做长周期历史类比 | 接受；或另找源（投资类数据源多需付费） |

---

## 6. 落地路线（分批，每批可独立验收）

### 批次 1｜零新增采集，只动展示层（**建议立即做**）

用**库里现有数据**把「外部约束」和「价格」两层做深：

| 交付 | 数据来源 | 工作量 |
|---|---|---|
| 新增 KPI：**中美 10Y 利差** + 近一年分位 | `bond_profit_daily` 两列相减 | 小 |
| 新增 KPI：**美债 10Y−2Y 利差** + 分位 | 同表已有列 | 小 |
| 新增 KPI：**美债 10Y 近一年分位** | 同表 | 小 |
| 「货币流动性」详情页新增分区「外部约束」 | 现有汇率数据 | 中 |

**为什么先做这批**：零采集成本、零新表、零 DQ 规则，
且 4.1 的结论（中美利差 0.4% 分位）**本身就是高信息量信号**——不落地可惜。

### 批次 2｜新增采集（中国数量维度 + 多国央行利率）

| 交付 | 新表 | 采集器 | 频率 |
|---|---|---|---|
| 中国货币供应 + 社融 + 信贷 + 准备金率 | `cn_liquidity_monthly` | 1 个采集器（4 个接口合一） | 月度（每月 10-15 日数据发布后） |
| 中国央行资产负债表 | `cn_cb_balance_monthly` | 1 个 | 月度 |
| 多国央行政策利率（美/欧/日/英） | `cb_policy_rate` | 1 个（4 个接口合一） | 日频（决议日才变） |
| 日韩欧股指（6 个指数） | 扩 `overseas_index_daily` | 扩现有采集器 | 日频 |
| ~~人民币中间价 26 币种~~ → **17 币种**（扩 `currency_boc_daily`） | 扩现有采集器 | 日频 |
| 美元指数（自算 + 快照） | `global_usd_index_daily` | 1 个 | 日频 |

**总计**：3 张新表 + 3 个新采集器 + 2 个采集器扩展 + 对应 DQ 规则 + 任务排期。

### 批次 3｜视图重构（在批次 1+2 数据就绪后）

把「货币流动性」详情页从「银行间利率页」升级为**分层流动性仪表盘**：

```
┌─ 全球层 ────────────────────────────────┐
│ 美元指数（自算）│ 全球央行方向（4 国决议）  │
├─ 美国层 ────────────────────────────────┤
│ 联邦基金利率 │ 美债曲线 │ 10Y−2Y │ 中美利差 │
├─ 中国层 ────────────────────────────────┤
│ 价格：Shibor 期限结构 │ DR007 │ LPR │ 国债曲线 │
│ 数量：M1−M2 剪刀差 │ 社融 │ 准备金率 │ 央行资产表 │
│ 结论：价×量四象限定位（主动宽松/需求不足/…）│
├─ 日韩欧层 ──────────────────────────────┤
│ 欧/日/英央行利率 │ DAX/CAC/FTSE/日经/KOSPI │
└────────────────────────────────────────┘
```

⚠️ **遵守上一轮已拍板的「不拆大卡」原则**：以上是**大卡内的分区**（用 `.card-sub` / `.card-cap` 做小标题），
不是新增卡片。卡片墙仍是 3 张 track 卡；若未来要新增，走 `registry.py` 契约流程。

---

## 7. 待拍板项（请逐条给结论）

> ✅ **2026-09-25 已全部拍板并落地**：**D1 = B**（批次 1+2）、**D2 = C**、**D3 = A**、**D4 = A**、**D5 = A**；
> **D6**（边界冲突）取方案 C「因果位置」划，见 §9。下表保留拍板时的选项与理由，供回溯。

| # | 决策项 | 选项 | AI 建议 |
|---|---|---|---|
| **D1** | 落地范围 | A) 只做批次 1（展示层，零采集成本）<br>B) 批次 1+2（含新增 3 表 3 采集器）<br>C) 全部（含批次 3 视图重构） | **B**：批次 1 信息量最高但太薄；批次 2 的中国数量维度是本主题的核心增量；批次 3 应等数据稳定后再改视图 |
| **D2** | 美元指数口径 | A) 自算（~~±3% 偏差~~ → **实为 +0.085%**，2016 起历史）<br>B) 只累积 `DINIW` 快照（准确但无历史）<br>C) 自算 + 快照标定（混合） | ✅ **已决策 C**，并**在 UI 上标注口径**（项目规范：口径必须随数字下发）。⚠️ 选项 A 描述的「±3% 偏差」是标价法单位错的假偏差，见 §2.1 勘误 |
| **D3** | 是否做「历史类比」 | A) v1 不做<br>B) 做但要标注「样本少、仅供参考」 | **A**：中国货币周期只有 3~4 轮完整样本，历史类极易过拟合，收益低于风险 |
| **D4** | 是否接 FRED API 补美国数量维度 | A) 不接，接受降级<br>B) 接（需申请 API Key，新增外部依赖） | **A**（本轮）：先用价格维度代理；等中国层做扎实后再评估 |
| **D5** | 韩国层缺口 | A) 接受（只用 KOSPI + 汇率）<br>B) 花时间找替代源 | **A**：影响量级小，不值得投入 |

---

## 8. 与现有体系的关系

| 现有模块 | 关系 | 边界（避免口径打架） |
|---|---|---|
| 市场风向（ERP / 股债性价比） | 流动性是 ERP 的**分母侧**因子 | ERP 用 `index_valuation_daily` + 国债 10Y；本模块用银行间利率与数量，**不重复算 ERP** |
| 跨市场对照（海外涨跌） | 与本模块「日韩欧层」共享 `overseas_index_daily` | 跨市场讲**收益联动**；本模块讲**流动性松紧**，同一数据两个视角 |
| 资金温度（汇率/发行/回购） | 「外部环境」卡已有汇率 | ⚠️ **存在重叠风险**：本模块「外部约束」层与「资金温度」的外部环境卡口径要划清（见 §9） |

---

## 9. 一个必须先解决的边界问题

**「资金温度」详情页已有一张「外部环境」卡（汇率 + 离岸人民币）**，
而本方案在「货币流动性」里也要放汇率与美元指数。**两处会打架**。

三种划法：

| 方案 | 划分依据 | 代价 |
|---|---|---|
| A) 按「变量类型」分 | 汇率归资金温度，利率/数量归货币流动性 | 逻辑清晰，但「美元指数」这个全球闸门会落空 |
| B) 按「区域」分 | 国内归货币流动性，跨境归资金温度 | 与现有「资金温度 = 汇率+发行+回购」结构冲突 |
| C) 按「因果位置」分 | **货币流动性管「钱的价格与数量」（因）；资金温度管「钱去哪了、多不多」（果）** | 需重写两边卡片的 `desc` 与判读条 |

**AI 建议 C**：货币流动性回答「央行与银行体系**投放/定价**了多少资金」，
资金温度回答「这些钱**有没有进入市场**」（基金发行 = 增量资金入场、回购 = 产业资本态度、汇率 = 外资流入）。
因果链更清楚，且不需要搬迁已有卡片。

### 9.1 ✅ 已落地（2026-09-25）

用户拍板方案 C，已固化到代码与契约：

| 落点 | 具体改动 |
|---|---|
| `registry.py` 契约 ⑩（新增） | **领域边界按「因果位置」划，不按数据源划**；给出判据（「给定条件」= 因 /「行为结果」= 果）与冲突实例（美元指数 vs 人民币汇率） |
| `registry.py` `money-cost` desc | 「回答**钱的价格与数量**（因）：价格、预期、政策、外部约束」 |
| `registry.py` `funding-temperature` desc | 「回答**钱去哪了、多不多**（果）：外部环境、增量资金、产业资本」 |
| `registry.py` `funding-temperature-fx` | `desc` 补「读的是跨境资金意愿的**结果**（果）；价格约束归货币流动性」；`question_text` 「外部资金环境松不松」→ **「外面的钱在进还是在出」** |
| `money_cost.py` `note` + docstring | 补「**领域边界**：本模块管钱的价格与数量（因）……归「资金温度」的是果」 |
| `funding_temperature.py` `note` + docstring | 对称补「本模块管果……归「货币流动性」的是因」 |
| `funding_temperature.py` `_verdict_fx` | 判读条去掉与「外部约束」撞名的「外部资金环境」，改为「汇率偏强，跨境资金在进不构成压制」 |

**验收证据**：`_scratch/_probe_cardtext.py` → V1~V4 **全 0/15 张卡**（新判读条 16 字）；
浏览器 DOM 量测 → 资金温度组第一张卡 `ask = 「外面的钱在进还是在出」`、`tone = ao-verdict--normal`。

---

## 附：本文引用的实测脚本（均留作长期复跑）

| 脚本 | 作用 |
|---|---|
| `_scratch/_probe_liquidity.py` | 全库 49 表按主题关键词三层匹配，列出候选表与列 |
| `_scratch/_probe_liq_cover.py` | 现有表真实覆盖（行数 / 起止 / 逐列非空率 / 最新值） |
| `_scratch/_probe_ak_liq.py` | 第一批 16 个 AKShare 接口实调（可用性 + 列名 + 行数） |
| `_scratch/_probe_ak_liq2.py` | 第二批 12 个接口实调 |
| `_scratch/_probe_liq_calc.py` | 实证算例（中美利差 / 自算 DXY / M1−M2 / 央行表） |
