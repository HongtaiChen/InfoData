# InfoData 全面修复与收口 · 新会话提示词

> 用法：把本文**整段复制**给一个全新会话（它没有本会话的历史），即可独立完成修复。
> 生成：2026-09-19 · 证据来源：`docs/未落地优化项盘点_2026-09-19.md`

---

## 一句话目标

在 `D:\Project\InfoData` 做一轮收口：把盘点报告里的未落地项**全部处置掉**——每一项必须是「修掉 / 明确废弃 / 问过用户后落地」三者之一，**不允许再留悬置项**。

> ## 📌 进度看板（2026-09-19 12:15 收口）
> **四批全部交付，无悬置项**（唯 3 项按提示词刻意留给用户拍板，见下方「待用户拍板」）：
> - **批次 A ✅** `96f8dcf`：A1 补登 `market_xcheck_daily` 元数据（43→44 行）/ A2 新增 `task_long_finished` 规则（命中 `daily_recon_window` 1259min、`financial_abstract_sync` 711min）/ A3 修复 TOCTOU 重复派发（接进程内互斥锁，8 线程实测 8 次→1 次）/ A4 `xcheck_*` 三条规则已覆盖。
> - **批次 B ✅** `07bf4e6`：修复快照表整表双写 + 本地派生补齐股本列 + B1~B6 六项决策定性。
> - **批次 C ✅** `004cea3`：5 张「只进不出」表 → **3 个分析模块**（钱贵不贵 / 跨市场对照 / 资金温度，后三者合并为一个模块）；`registry` 2 → **5**；卡片墙 5 卡 3 行零留白。7 处口径坑与 3 个视觉缺陷在实现中实测发现并修掉。
> - **批次 D ✅** `e3a92cc`：D1 开机资讯回看 / D2 `index_constituents` 查清（月频、按日留档、不回补）/ D3 源列脏前半保留 + **后半定性推翻并修根因**（概念榜跨日/跨名拼接，实测 373 条从 370 条为空 → 0 空值）；D4 按计划不动。
> - **第二轮补修 ✅** `7365701` + `f749ad4`：**独立复核**四批交付（结论：全部真落地、无虚报），另挖出 2 个真实缺陷 + 3 处数字漂移并处置 → 见 **§5**。要点：「整链拓扑」当轮实录**全空**（`run_steps` 层级错位，44 表/35 任务，实录有值任务 **0 → 33**）；失败现场补 `error_trace` 栈帧留档。**§5.3 含一条自我纠错**（曾把 `index_valuation_sync` 的 TypeError 误判为「未修偶发缺陷」）。
>
> **工具/规则口径变更**：分析模块 2→5；DQ 规则 107→**118**；`table_meta` 44 行（8 张表 `flow_desc` 重写）；新增 1 项启动行为（资讯回看），无新增任务/班次。
> **⚠️ 待用户拍板（刻意不擅自动手）**：① 回收 1.92 GB 备份表（B5）；② `change_pct` 源侧缺口（修源 or 废弃该列，新增 DQ 规则会持续显示 fail）；③ `stock_market_daily_ex` / `capital_flow_sync` 是否最终归档。
> **⚠️ 已知会持续报警的一条**：`concept_change_pct_gap`（体检实测 fail 99.196%）——**这是如实口径、不是误报**，指向源侧真实缺口；分析层已有 `close` 兜底，无用户可见影响。**不要为「让它变绿」放宽阈值**（§4 边界明令禁止）。
>
> ~~工作区 5 个未提交文件~~（批次 B 已由并行会话交付 `07bf4e6`）
> ⚠️ **同一仓库可能同时有多个会话在改**：动手前先 `git status` + `git log --oneline -3`；**只 `git add` 自己改的路径，别用 `git add -A`**；重启后端会让对方的后台任务报 `failed`，那是预期不是故障。

## 第一步：先读这三份材料（读完再动手）

1. `D:\Project\InfoData\docs\未落地优化项盘点_2026-09-19.md`（待办清单 + 证据）
2. **本文 §0 勘误**——报告里有 4 条结论已被复核推翻/纠正，**别去修不存在的问题**
3. 相关技能：`investbuddy-add-derived-column`（新增分析模块全链路）、`investbuddy-gap-inventory`（横向拉网）、`investbuddy-backend-restart`（启停）、`investbuddy-dq-audit`（数据质量）、`investbuddy-scheduler-health`（调度诊断）

---

## 0. ⚠️ 勘误：报告中 4 条结论失实/判错（2026-09-19 逐条复核）

| 报告原文 | 复核结论 | 证据 |
|---|---|---|
| §4-① `daily_ohlc_consistent` 规则**永久 error**，DQ 白名单缺 `LEAST`/`GREATEST` | ❌ **早已修复，不要动** | `data_quality_check.py` 的 `_WHERE_FUNCS`（第 72-73 行）已含两者（注释「09-10 补充」）；该规则 09-10 19:40 起连续 3 次 `pass`；近 7 天全库 `status='error'` 规则 **0 条**。报告读到的是 09-10 19:32 那条**历史记录**，把一条已愈合的伤口当成了活伤 |
| §3.2 「单次运行 >3h **无任何告警覆盖**」 | ⚠️ **表述不准，但缺口真实存在（换了位置）** | 已有 `task_stale_running(hours=3)` 规则且一直 pass。真实缺口 = 它只覆盖**未收尾的 running**，覆盖不到**已完成但耗时超长**的运行：`daily_recon_window` 09-17 21:15→09-18 18:14 **success 1259 分钟**、`financial_abstract_sync` 09-16 06:52→18:43 **success 711 分钟**（疑为机器待机冻结进程所致） |
| §3.2 长耗时表格（9741 / 6515 / 5121 分钟…） | ❌ **11 条里 8 条是「假的超长」** | 那些记录 `error_message` 明写「进程重启中断，已由运维脚本标记为 failed（非任务本身失败）」——时长 = `started_at → 收尾时刻` 的跨度，**不是真实执行时长**。判「任务是否真的慢」只能看 `status='success'` 的记录 |
| §3.3 「链式/兜底重复派发（`_succeeded_today` 未生效）」 | ❌ **判断错，真因已定位并修复** | ① `_succeeded_today` **完全生效**——兜底班次 21:30/21:40/21:50 近 14 天**一次都没执行**；② 真因是 `_execute` 的 **TOCTOU 竞态**：链式线程 / catchup 线程 / 调度 worker **同进程内并发**同时通过 `_running_count`（真正 INSERT running 在 `run_task()` 内部，中间无互斥）。现场：09-19 `07:38:18 market_current_sync ×2`、`07:38:40 data_quality_check ×3`；09-18 `19:00` 各 ×2。→ 已由批次 A 修复，见 **A3**（含「曾误判为多进程」的纠错） |

**另有两处报告偏差，一并纠正：**

- **§2.3 低估了 `capital_flow_sync`**：报告说它是「决策问题不是开发问题」。读源码 `run.py:607-613` 才发现禁用的真实原因是**东财域名在本机不可达**，且「主力净流入 = 超大单+大单」口径仅 90~93.8% 成立 → 「启用」这条路**可能根本走不通**，别默认它会跑通。
- **报告漏了一条**：`ai_concept_analysis` 也是 `enabled=0`（cron = 「手动」），且它**调用豆包 AI 会产生费用**。清单里必须一并处置。

---

## 1. 环境与铁律（违反任何一条都会返工）

**体系**
- FastAPI 后端 `127.0.0.1:8000`（**无 --reload，改后端必须重启**）；Vue3 + Vite 前端 `127.0.0.1:5173`（HMR，改前端不用重启）
- 数据访问统一走 `app/db.py` 的 `query_all` / `query_one` / `execute_write`
- **种子脚本是唯一事实来源**：任务 `seed_task_config.py`、规则 `seed_dq_rules.py`、表元信息 `seed_table_meta.py`。直改 DB 后**必须回写种子脚本**，否则下次 seed 覆盖回去

**必须遵守的既有坑**
1. **先读 `information_schema.COLUMNS` 再拼 SQL**。2026-09-19 又踩一次：凭记忆写 `task_config.cron_expr` → `Unknown column`。真实列名是 `cron`（`task_config` 仅 `task_name / enabled / cron / params / update_time`）
2. `query_one` **自带 `LIMIT 1`**，SQL 里再写会拼成 `LIMIT 1 LIMIT 1`
3. `query_all` 传 `params=None` 时 pymysql 不做 `%` 转义 → SQL 里写 `%Y` 即可；**只有传 params 时才写 `%%`**（写反会静默返回空结果）
4. `information_schema.TABLE_ROWS` 是**估算值**（会把 36 读成 35、107 读成 104）→ 判一致性必须 `SELECT COUNT(*)`
5. 改采集器后必须**真实 import 验证**（`py_compile` 查不出导入期 NameError）
6. 同一文件**严格串行 Edit**（并发写会互相覆盖，工具却报成功）→ 改完 grep 复核落盘
7. 新增/改动主链任务必须**同步 `scheduler.CHAIN_NEXT`**；cron 时刻须落 **19:00–23:00** 窗口；`day_of_week` **0 = 周一**（与 Unix 反向）
8. 若目标表被 `DELETE+INSERT` **整表重建**，外来列会被清空 → 不能挂列，必须独立建表
9. 沙箱 Bash 会丢 coreutils PATH（`ls/tail/head` 报 command not found）→ 文件操作改用 python 绝对路径 `C:\Users\cht\.workbuddy\binaries\python\versions\3.13.12\python.exe`；涉及网络的脚本必须先 pop 掉全部代理变量
10. 启停服务用技能 `investbuddy-backend-restart`（后台 + 沙箱外 + 无代理），**停完复查端口**
11. ⚠️ **后台任务报 `failed` ≠ 服务挂了**（2026-09-19 实测：端口由另一 PID 正常服务、调度器健康、代码是最新的）——常见原因是**另一个会话按流程重启了后端**。收到失败通知先核 PID 与代码版本，别急着重启
12. ⚠️ **同一仓库可能被多个会话并发操作**（2026-09-19 当天就发生了：提示词写完时，另一个会话已交付批次 A 的 `96f8dcf`）→ 动手前先 `git log --oneline -3`；**只 `git add` 自己改的路径，别用 `git add -A`**（会卷进对方未完成的改动）；读代码做归因时必须确认读的是**出问题那一刻的版本**（`git show <commit>:<file>`）——当天踩过：拿**修复后**的代码推断**修复前**行为，得出错误根因

**验收口径（每批结束都要过一遍）**
- `py_compile` + 真实 import
- `node node_modules/vue-tsc/bin/vue-tsc.js -b --force` 退出码 0
- 接口冒烟：`/api/health` + 改动的端点（用 python urllib 且 **pop 掉代理变量**；沙箱 curl 会 502）
- 前端截图用 `frontend-shot-puppeteer` 技能，**等待条件必须覆盖全部卡片**（只等第一张会截到半成品）
- `git commit`（一批次一提交）+ `push`

---

## 2. 批次与处置要求

> 建议顺序 A → B → C → D。**若单会话做不完，至少完成 A + B**；每批都是可独立交付的最小单元。

### 批次 A：低风险高确定性（先做）

| # | 事项 | 动作 | 验收 |
|---|---|---|---|
| **A1** | `market_xcheck_daily` 未登记 `table_meta`（7 张新表里唯一漏网） | 在 `seed_table_meta.py` 补一条（分类「质量」，`flow_desc` 写明「`xcheck_sync` 每日 22:30 写入」），执行 seed。列名：`table_name / category / source_desc / flow_desc / writers / writer_cols / note` | 数据页能看到该表说明；`table_meta` 43 → 44 行 |
| **A2** | 「**已完成**但耗时超长」的运行无告警 | 新增一条 DQ 规则（参照已有 `task_stale_running` 的写法，查 `task_runs`）：`status='success'` 且 `TIMESTAMPDIFF(MINUTE, started_at, finished_at) > 180`，severity 建议 `warning`。**规则描述要写清**：这类超长多为「机器待机冻结进程」所致，是**物理离线的信号**，不是任务本身慢 | 手动跑一次体检，规则有结果且不误报 |
| **A3** | ✅ **同秒重复派发（已闭环，提交 `96f8dcf`）** | 根因**既不是**链式去重失效、**也不是**多进程：是 `_execute` 的「先查后跑」(TOCTOU)——链式线程 / catchup 线程 / 调度 worker **同进程内并发**同时通过 `_running_count`，而真正 INSERT running 发生在 `run_task()` 内部。类里早已声明 `_locks/_lock_guard` 却**从未接线**（死代码）。现场：09-19 catchup 串行补到 `market_style_sync` → 起链式线程接力 `market_current_sync`，而 catchup 队列下一步**也是**它 → 双份；两份又各接力 `data_quality_check`（+2），加上 catchup 队列里的它自己（+1）= **3 份并发** | **已修**：`_execute` 接上按 task_name 的进程内非阻塞锁（8 线程实测：修复前执行 8 次 → 修复后 1 次；3 个不同任务仍并行，0.50s）。**残留观察项**：跨进程仅靠 DB 层 `_running_count` 兜底——但实测 Windows 下两个 uvicorn **无法**同时 bind 同一端口（`SO_REUSEADDR=1`→errno 13、默认→errno 10048，均失败），不存在第二个监听者，风险可接受 |
| **A4** | `xcheck_*` 三条新规则「未产生结果」 | 确认下轮体检已覆盖即可（属「还没轮到」，**不需改代码**） | 体检后 `dq_report` 出现 3 条结果 |

### 批次 B：决策项（**先用 AskUserQuestion 问，不许擅自删数据 / 花钱**）

| # | 事项 | 要问的问题 | 建议默认 |
|---|---|---|---|
| **B1** | ✅ **已闭环（`07bf4e6`）** — `stock_market_current` 由「8 列全空」降到「**6 列全空**」：`volume_ratio / dynamic_pe / pb / rise_speed / 5m_change_pct / 60d_change_pct`；表体 **5,121 行**（原记 10,242 行＝整表双写事故的计数，已随同批修复）。`total_captital / float_captital` 已由本地 `stock_shares` **派生补齐**（5,121/5,121 非空）。余 6 列根因＝日线聚合口径产不出东财实时字段（`SELECT` 只 14 列而 `INSERT` 硬列了这 8 列） | ~~接实时源 还是 删列？~~ 已定：**2 列本地派生 + 6 列元数据定性**，不删列 | **先实测东财实时接口在本机是否可达**（参照 `capital_flow_sync` 的不可达先例）。可达 → 接源；不可达 → **删列**，并同步改 `market_current_sync.py` 的 INSERT 列清单 |
| **B2** | `capital_flow_sync`（`enabled=0`，cron `15 22 * * *`） | 启用 还是 废弃归档？ | 源码注释已写明东财不可达 + 口径仅 90~93.8% 成立 → 倾向**明确废弃并归档**。若实测可达再谈启用，**且必须改 cron**：`15 22` 与 `financial_abstract_sync` **完全撞车** |
| **B3** | `ai_concept_analysis`（`enabled=0`，cron = 手动） | 是否启用？**会调用豆包 AI 产生费用** | 涉及花钱，必须用户拍板 |
| **B4** | `stock_market_daily_ex`（16,891,313 行 / **3,196.9 MB**，停更一年，无任何任务写它） | 归档 还是 保留？ | 倾向归档：先导出压缩 + 校验行数，再 drop |
| **B5** | 备份表占 1.92 GB（`stock_market_daily_bak_20250802` 1905 MB 等） | 是否回收？ | **必须先列出全部受影响表名与体积、导出留档、并获用户明确同意**后才可动 |
| **B6** | `securities_margin.rzrqyecz` 近 6 个交易日恒 0.9778（非取舍，是核实） | — | 查 akshare 接口文档 + 与同表其他列对账，给出「这一列到底是什么」的定论，并把结论写进 `table_meta.flow_desc` |

### 批次 C：消费端补齐（**工作量最大、收益最高，本轮的核心价值**）

报告 §1 的 5 张表「只进不出」：`interbank_rate_daily`(4,984 行) / `overseas_index_daily`(20,367) / `currency_boc_daily`(5,615) / `stock_repurchase`(5,513) / `fund_new_issue`(6,848)。目标：**变成分析研究页上看得见的结论**，同时填掉板块轮动卡右下留白。

- **C1（优先）** `interbank_rate_daily` → 新模块「钱贵不贵」（Shibor 期限结构 / LPR 变动 / 与股指的关系）
- **C2（优先）** `overseas_index_daily` → 新模块「跨市场对照」（美股 / 港股 / 日欧 vs A 股，隔夜效应）
- **C3（合并）** `currency_boc_daily` + `fund_new_issue` + `stock_repurchase` → 合并为**一个**「资金温度」模块（汇率 / 新发基金 / 回购，都是「钱往哪去」的侧写）。**不要做三个信息量不足的独立卡片**

每个新模块必须**完整走完链路**（参照技能 `investbuddy-add-derived-column`）：
1. `analysis/<module>.py`：计算逻辑 + KPI + verdict
2. `analysis/registry.py`：注册条目（`key / kind / title / desc / card_span / ...`）
3. `api/analysis.py`：路由
4. `frontend/src/views/analysis/<Module>View.vue` + 总览页卡片接入
5. `seed_table_meta.py`：补数据来源说明
6. `seed_dq_rules.py`：补新表的数据质量规则

**必须遵守的卡片墙契约（5 条）**
- `verdict` 由**后端生成**（一句人类可读的结论，不许前端拼）
- `card_rank` 决定 KPI 排序与取舍；**整行卡放 6 个 KPI、半宽卡放 3 个**（多放会被截断）
- `desc` 写口径（来源 / 口径 / 窗口）
- `tone` 三态：`updown`（红涨绿跌，**只给真正的行情数字**）/ `diff`（组间收益差，主色蓝 + 保留正负号）/ `neutral`（无量纲，主色蓝）。**卡片墙的颜色只表达异常程度、不表达方向**；极值才染金
- `scale`（有历史序列才给）：`{pct, label}`，**`label` 必须后端下发**（如「近 250 个月末」），**禁止前端写死「近一年」**（会把月频分位说成日频）

⚠️ 判定「某状态是常态还是异常」**必须用 ≥ 1 年的回放窗口**。本项目踩过：16 日窗口把 99.6% 分位的极值误判成「常态」。

### 批次 D：结构性（只做能做的，别硬上）

| # | 事项 | 动作 |
|---|---|---|
| **D1** | `news_fetch` 命中率仅 ~26% | **不是 bug**，是家用电脑物理离线的结构性后果（不丢数据，靠回看补采兜底）。可做优化：确保开机 `catchup_missed` 完成后**追加一次新闻回看**。改完说明预期改善幅度，**不要承诺解决** |
| **D2** | `index_constituents` 仅 2 个快照日 | 先查清 `index_cons_sync` 的真实频次（cron `30 21 15 * *` = **月频**）与写入语义，再决定「回补历史快照」还是「按频次自然积累」——月频的话 2 个快照日属正常起步 |
| **D3** | 源列脏（`turnover_ratio` >1、概念异常收益 +110%） | 属可接受技术债（分析层已绕行）。**只记录、不修改**；顺手可加一条 DQ 规则做监控 |
| **D4** | 「唤醒看门狗」观察期 | **本轮不动**。观察期到 **09-24** 才复盘 |

---

## 3. 交付要求

- 每批次结束：`git commit` + `push`，提交信息写清「改了什么 / 为什么」
- 全部结束后更新 `docs/未落地优化项盘点_2026-09-19.md`：已处置项标记「✅ 已闭环 + 提交号」，**并修掉 §0 勘误里的四条失实/判错结论**
- 更新记忆：当日日志（`2026-09-19.md` 或新日期）；若有新的硬约束则更新 `MEMORY.md`
- 过程中发现的新坑，写进对应技能
- 最终交付**收口报告**：处置清单（每项含 做了什么 / 提交号 / 验收证据）+ 仍悬置项（含原因）+ 运维口径变更（任务数 / 规则数 / 新表）

## 4. 边界（不要做）

- 不改电源 / 待机设置，不上云，不做 RTC 定时唤醒（已评估：零收益、多故障点）
- **不删除任何库表或列**，除非该决策项已获用户明确同意
- 不为了「看起来完成」而放宽 DQ 阈值或删除规则

---

## 5. 第二轮补修（2026-09-19 下午 · 独立会话复核后）

> 起因：对本文四批交付做**独立复核**——不信文档、只信实库实码。
> 结论：四批 A/B/C/D **全部真落地、无虚报**；复核过程另行挖出 **2 个真实缺陷 + 3 处数字漂移**，本节记录处置。
> 提交：`7365701`（后端）、`f749ad4`（前端）。

### 5.1 ✅ 修掉「整链拓扑」当轮实录全空（本轮最有价值的修复，`7365701`）

**现象**：数据中心「数据流明细」的每一步右侧都渲染成「—」。实测接口 `/api/db/tables/flow`：
44 张表 / 35 个任务的模板步骤齐全，但实录 `run_steps` **恒为 `null`**。

**根因（层级错位，三处各写了一半）**：
1. 写入侧 `_common.with_steps()` 把步骤链放进 **`result["run_detail"]["run_steps"]`（第二层）**；
2. `app/tasks/run.py::_set_run_detail()` 又把这个 `result` **整包**落库 →
   `task_runs.run_detail` 顶层只剩 `records_written / error_count / errors / note`，步骤链被压在第二层；
3. 前端 `DataView.vue` 读的是**第一层** `last.run_detail.run_steps` → `real` 恒为空数组 → 每步渲染「—」。

→ 后果：「模板 + 实录对照」这个特性**只兑现了一半**（步骤名/参数有，当轮实测值全空）。

**修复（两侧夹击）**：
- 写入侧新增 `run.py::_flatten_run_detail()`：落库前把内层 `run_detail` 的键**提到顶层**
  （外层 `duration / source_stats / cutoff / skipped / as_of / summary` 全保留——`stock_daily_incr`、
  `xcheck_sync` 的额外键都在外层，前端要用）；
- 读取侧 `api/db_browser.py::_parse_run_detail()`：对**存量行**读时展平，
  让尚未重跑的任务（月频 `index_cons_sync`、手动 `ai_concept_analysis`）立刻显示实录值，不必等下一轮。

**验收（实测，非推断）**：重启后端（**后端无 `--reload`，改代码必须重启**）后调 `/api/db/tables/flow`：

| 口径 | 修前 | 修后 |
|---|---|---|
| 有模板的任务数 | 35 | 35 |
| **实录有值的任务数** | **0** | **33** |
| 实录步骤总数 | 0 | **142** |

样例（`bond_profit_daily` / `bond_profit_sync`）：step1「拉取中美收益率」→
`接口 9346 天（2026-09-07 ~ 2026-09-18）`；step4「Upsert 回填」→
`upsert 10 天 · 新增 0 · 回填美债 9 / 中债 0（残留缺口 美债 1 / 中债 0）`。
余 2 个任务无值属正常（其步骤本就未产出实录）。

### 5.2 ✅ 顺带修掉：实录 chip 把步骤名挤成逐字竖排（`f749ad4`）

5.1 修好后，实录 chip **第一次真正渲染出来**，随即暴露一个布局缺陷：
长实录值（如 `整表至 2026-09-18；中债腿至 2026-09-18；…`）与步骤名争抢同一行——
名称 span 只有 `min-width:0`、**没有 `white-space:nowrap`**，而 CJK 可在任意字符处断行，
于是被压到 min-content = 1 个字宽，渲染成「两/腿/独/立/水/位/线」**逐字竖排**，
行高 29px → 121px，chip 顶进右侧「字段血缘落点」面板。

**修法**（单任务三段整链 + 多链路两条车道，两处结构相同一起改）：名称加
`flex:1 1 auto` + `white-space:nowrap` + `text-overflow:ellipsis`；实录 chip 加
`flex:0 1 auto` + `max-width:62%` + 省略号截断 + `:title="s.value"`（悬停看全文）。

**验收**（puppeteer **DOM 量测**，不靠目测）：4 行 行高 **29px / 名称高 19px**（单行）、
疑似挤压行 **0**、实录 4/4 有值、运行时错误 0、`vue-tsc -b --force` 退出码 0。

### 5.3 ✅ 失败现场补栈帧留档（`7365701`）—— 并更正一条**误判**

**先纠错**：复核时曾把 `index_valuation_sync` 09-19 07:57:44 的
`TypeError: tuple indices must be integers or slices, not str` 判为「重试即好的**未修**偶发缺陷」。
**这个判断是错的**，三条证据推翻：
1. 该采集器唯一提交 `703ecba` 的时间是 **08:07:50**，比失败时刻**晚 10 分钟** → 那次失败跑的是**开发期未提交版本**；
2. 全库 `task_runs` 扫描该报错**仅出现 1 次**（成功重跑在 07:58:43，修复后 55 秒内手跑）；
3. 静态扫描 87 个后端文件（作用域感知，`_scratch/scan_dict_risk.py`）：
   「**裸游标 + 按列名取值**」真隐患 **0 处**；当前 `index_valuation_sync.py` 三处连接**全部显式 `DictCursor`**；
   另连跑采集器 3 次，0 异常（上游偶发抖动由容错吃掉，不抛错）。

**但真问题还在**：失败只存一行 message、**没有栈帧**——这类 4 秒就挂的偶发错误根本查不动
（本轮就为此白花一轮排查）。**修复**：`run.py::_error_trace()`
→ 失败时把「异常类型 + `backend/` 内栈帧（文件:行 函数 代码，已过滤 site-packages）」写入
`task_runs.run_detail.error_trace`（**附加字段**，前端按可选字段读，零侵入）；
失败分支由 `logger.error` 改 **`logger.exception`** 保留完整堆栈。

**验收**（失败路径冒烟，全程**不写库**：`TaskRecorder` 换假实现 + 覆盖执行体制造同款 TypeError）：
落库载荷实测 =
`{"error_trace":{"type":"TypeError","message":"tuple indices must be integers or slices, not str",
"frames":[{"file":"app/tasks/run.py","line":867,...},{"file":"app/collectors/index_valuation_sync.py","line":209,...}]}}`；
成功路径实跑 `index_valuation_sync` → `status=success` / +1281 条 / `run_steps` 完好。

> ⚠️ **另一处日志缺口（仅记录，本轮不改）**：`backend/logs/*.log` 全部 **0 字节**。
> 后端实际以 `uvicorn app.main:app`（**无 `--reload`**）启动，stdout 未落盘。
> 即**当日志都没落盘时，`task_runs.run_detail.error_trace` 是唯一的持久化失败现场**，别再把它砍掉。

### 5.4 ✅ 三处数字漂移（已按实库更正）

| 位置 | 原记 | 实测（2026-09-19 下午） | 口径 |
|---|---|---|---|
| 本文 §B1 / 盘点 §2.1 | 8 列全空 / 10,242 行 | **6 列全空 / 5,121 行** | `COUNT(*)` 实算；差值恰为一份完整快照（双写事故计数） |
| 本文 §B4 | 1.7 GB | **3,196.9 MB**（16,891,313 行） | `DATA_LENGTH + INDEX_LENGTH` |
| 盘点 §2.3 | 95.6 MB | **147.2 MB** | 同上（原值疑只算 `DATA_LENGTH`） |

> 复现口径：体积取 `information_schema.TABLES` 的 `DATA_LENGTH + INDEX_LENGTH`；
> 行数一律用 `COUNT(*)` 实算（`TABLE_ROWS` 是估算值，会误导）。

### 5.5 ⏳ 仍悬置（未擅自动手，均有原因）

| 项 | 原因 |
|---|---|
| 备份表回收（1.92 GB，§B5） | 需用户明确同意（§4 边界明令禁止自行动手） |
| `change_pct` 源侧缺口（修源 / 废弃列） | 决策项 |
| `stock_market_daily_ex` / `capital_flow_sync` 最终归档 | 决策项 |
| `market_style_daily_bak_20260914` 是否补登 `table_meta` | 依赖上一条「备份表回收」的结论；且三张 bak 表中另两张已登记，需统一口径 |

**本轮无新增/删除任务，无 DQ 规则数变化**（`task_config` 仍 35 行、实际排 33；DQ 仍 118 条）。
仅新增 1 个 `run_detail` 附加字段（`error_trace`）与 1 处前端样式修正。
